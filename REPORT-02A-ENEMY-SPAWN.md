# REPORT-02A — 적 시스템 · 스폰 시스템 분석

분석 대상 파일: `my-first-code/index.html`
git blob: `3a9a6d26956dc9ad6c1ede566506535e68640b93`
파일 크기: 1,522,060 bytes / 7,736 lines
시뮬레이터: `/tmp/spawn-sim.js` (실행 완료, 출력 `/tmp/spawn-sim-out.txt`)

> 이 보고서의 모든 줄번호는 위 blob 기준 `my-first-code/index.html`의 실제 줄번호다.
> **게임 코드는 한 줄도 수정하지 않았다.** (§20 확인 결과 참조)

---

## 1. 분석 범위

### 포함
- 적 정의 전수 조사 (`ENEMIES`)
- 적 역할군 (`ENEMY_ROLE`)
- 위협 예산 (`ENEMY_THREAT_COST`, `THREAT_BALANCE`, `refreshThreatBudget`)
- 웨이브 구성 (`WAVE_COMPOSITION`, `waveRolesFor`, `roleUnlocked`)
- 스폰 디렉터 (`spawnDirector`, `spawnThreatWave`, `spawnEnemy`)
- 스폰 간격 공식
- hardCap 병목
- 실제 적 조합 (시뮬레이션 기반)

### 제외 (지시에 따라 분석하지 않음)
보스 / 미니보스 AI, 대원(캐릭터), 무기, 패시브, 메타 성장, 스테이지 변이, 차수 전체 구조.

`bossCut`·`miniCut`은 **스폰 예산 계산에 들어가는 값이므로 그 사실만 기록**하고 (§5), 보스 시스템 자체는 분석하지 않는다.

### 저장소 구조 (실제 확인)
```
git repository root : /home/user/test
my-first-code/      : 저장소 루트의 하위 디렉터리 (origin/main 브랜치에 존재)
my-first-code/index.html : 7,736 lines / 1,522,060 bytes
```
현재 작업 브랜치(`claude/game-update-ztrs5p`)에는 동일 내용이 저장소 루트의 `index.html`로 존재하며, 두 파일의 blob 해시가 `3a9a6d2…`로 **완전히 동일**함을 확인했다. 분석은 `my-first-code/index.html`을 `/tmp/target.html`로 읽기 전용 추출하여 수행했다.

---

## 2. 실제 적 개수

개수를 가정하지 않고 `ENEMIES` 객체(`index.html:2456`)의 키를 직접 세었다.

| 구분 | 개수 | 목록 |
|---|---|---|
| `ENEMIES`에 정의된 적 | **15종** | walker, runner, brute, bomber, spitter, howler, armorwalker, archer, reaper, slime, slimeling, necromancer, stonegolem, lich, firedemon |
| `ENEMY_THREAT_COST`에 비용이 있는 적 | **15종** | 위와 동일 (누락 없음) |
| `WAVE_COMPOSITION`에 실제로 들어 있는 적 | **13종** | walker, runner, brute, bomber, armorwalker, spitter, slime, howler, reaper, necromancer, stonegolem, firedemon, lich |
| 위협 예산 스폰으로 **절대 나올 수 없는 적** | **2종** | `slimeling`, `archer` |

- `slimeling`은 슬라임 사망 분열로만 생성된다 (`index.html:4645`).
- **`archer`는 어떤 경로로도 생성되지 않는다.** 상세는 §13 문제 2.

Tier별로 300초 안에 실제로 뽑힐 수 있는 적 종류 수 (시뮬레이터 §8 출력):

| Tier | 실제 등장 가능 종 수 | 목록 |
|---|---|---|
| 1 | **5종** | walker, runner, brute, bomber, spitter |
| 2 | **6종** | + armorwalker |
| 3 | **8종** | + slime, reaper |
| 4 | **11종** | + howler, necromancer, stonegolem |
| 5 | **13종** | + firedemon, lich |

---

## 3. 적 전수 조사

수치는 전부 `ENEMIES`(`index.html:2456-2472`)와 `ENEMY_THREAT_COST`(`index.html:3042`)의 실제 값이다.
등장 조건은 `WAVE_COMPOSITION`(`3057`) + `roleUnlocked()`(`3072`)에서 도출했다.

| 적 | 역할(`ENEMY_ROLE`) | HP | 공격력 | 이동속도 | 공격 방식 | 특수능력 | **cost** | 등장 조건 | 플레이어 대응 |
|---|---|---|---|---|---|---|---|---|---|
| walker | chaser | 20 | 8 | 0.72 | 접촉 | 없음 | **1** | t≥0 (t<200까지) | 이동만 하면 됨 |
| runner | chaser | 15 | 6 | **1.7** | 접촉 | 없음 | **2** | t≥0 전 구간 | 지속 이동·거리 유지 |
| slimeling | chaser | 10 | 6 | 0.95 | 접촉 | 없음 | **1** | 스폰 불가 (분열 전용, `4645`) | 잔몹 정리 |
| slime | area | 36 | 10 | 0.6 | 접촉 | 사망 시 slimeling ×2 분열 (`4644`) | **5** | t≥100 **AND** tier≥3 | 광역기로 한 번에 / 분열 후 처리 각오 |
| spitter | ranged | 20 | 10 | 0.6 | 원거리 산탄 (`spitAcid` `5186`) | 35% 확률 2발 (`ENEMY_BAL.spitter.twinChance`) · **220 밖이면 접근, 170 안이면 후퇴** (`4890-4899`) | **4** | t≥100 | **투사체 회피** + 우선 제거 (직접 접근이 안 됨) |
| archer | ranged | 24 | 12 | 0.72 | 원거리 (spit 플래그) | 없음 | **4** | **없음 — 도달 불가** | — |
| bomber | suicide | 25 | **30** | 1.05 | 접촉 자폭 | 접촉 시 34프레임 전조 후 반경 70 폭발 (`ENEMY_BAL.bomber`, `5176`) | **5** | t≥60 | **전조 보고 반경 밖으로 이탈** |
| brute | tank | 60 | 18 | **0.5** | 접촉 | 없음 (HP·공격력만) | **7** | t≥30 | 뒤로 빠지며 사격 (느려서 무시 가능) |
| armorwalker | tank | **95** | 14 | 0.5 | 접촉 | **정면 ±70° 피해 55% 감소**, 장갑이 초당 1.68° 회전 (`enemyArmorMul` `2848`, `ENEMY_BEHAVIORS.armorwalker` `2873`) | **7** | t≥60 **AND** tier≥2 **AND** t≥25 | **측면·후방으로 파고들기** |
| howler | support | 34 | 12 | 0.62 | 접촉 | **반경 260 아군 전체 이동속도 ×1.4 (240프레임)** + 반경 150 플레이어 둔화 (`howl` `5201`) · 125~165 거리 유지 (`4904`) | **8** | t≥150 **AND** tier≥4 | **최우선 제거** (거리 유지형이라 추격 필요) |
| firedemon | area | 82 | 20 | 0.85 | 접촉 + 장판 | 추격하며 26~44프레임마다 화염 장판 (`dropFire` `5197`) + 이동 방향 46 앞을 예측 점화 (`ENEMY_BEHAVIORS.firedemon` `2911`) | **9** | t≥250 **AND** tier≥5 | **지역 회피 + 이동 경로 꺾기** |
| necromancer | support | 64 | 12 | 0.46 | 없음(원거리 유지) | walker ×2 소환 (280~400f, `necroSummon` `5192`) + 시체 부활 최대 2구 (`raiseDead` `2921`) · 190~240 거리 유지 (`4910`) | **10** | t≥200 **AND** tier≥4 | **우선 제거 + 추격** (안 잡으면 무한 증식) |
| lich | ranged | 74 | 16 | 0.5 | 원거리 | spit + **시체 부활** (`ENEMY_BEHAVIORS.lich` `2910`) | **10** | t≥250 **AND** tier≥5 | 투사체 회피 + 우선 제거 |
| reaper | assassin | 42 | 20 | **1.5** | 접촉 | **36프레임 전조 후 측면 150 거리로 점멸** (`ENEMY_BEHAVIORS.reaper` `2879`) | **11** | t≥150 **AND** tier≥3 | **전조 원 위치를 보고 미리 회피** |
| stonegolem | tank | **170** | 22 | **0.4** | 접촉 + 충격파 | 3단 전조(작게→크게) 후 반경 190 지면 충격파 26피해 (`ENEMY_BEHAVIORS.stonegolem` `2895`) | **12** | t≥200 **AND** tier≥4 | **전조 반경 이탈** (느려서 추격 부담은 없음) |

### 3-1. threat cost와 실제 위험도의 불일치

#### (A) cost는 높은데 플레이어 대응이 단순한 적

| 적 | cost | 근거 |
|---|---|---|
| **brute** | **7** | 특수 행동이 전혀 없다. `ENEMY_BEHAVIORS`에 항목이 없고 (`2871-2919`), `ENEMIES.brute`에 `bomb/spit/howl/summon/firetrail/split` 플래그가 하나도 없다 → `updateEnemies`의 마지막 `else` 분기(`4915`)로 떨어져 **직선 추격만** 한다. 속도 0.5는 플레이어 기본 이동속도 2.7(`makePlayer`, `baseSpd:2.7`)의 **18%**다. 즉 걷기만 해도 절대 닿지 않는다. cost 7은 순수하게 HP 60 + 공격력 18에서 온 값이다. |
| **stonegolem** | **12** (최고) | 충격파 전조는 있으나 속도 0.4(플레이어의 **15%**)이고 발동 조건이 `d > 420이면 return`(`2899`)이다. 플레이어가 420 밖에 있으면 아무것도 하지 않고, 접근할 이유도 없다. 전 적 중 **가장 비싼 적이 가장 무시하기 쉬운 적**이다. |
| **armorwalker** | **7** | 장갑 회전 속도가 `turn:0.028 rad/frame` = **초당 96°**다. 플레이어가 적 주위를 도는 속도가 이보다 빠르면 장갑은 계속 뒤처진다. 속도 0.5로 추격도 못 하므로, 실질 대응은 "옆으로 조금 걷기" 하나다. |

#### (B) cost는 낮은데 이동/공격 방식 때문에 위험한 적

| 적 | cost | 근거 |
|---|---|---|
| **runner** | **2** | 속도 **1.7**로 플레이어(2.7)의 63%. 무리 지어 오면 실제로 포위가 된다. cost 2는 HP 15·공격력 6에서 왔지만, **위협의 본질은 속도**다. §8 결과에서 runner는 전 tier 최다 스폰 종(28~33%)이다. |
| **bomber** | **5** | 공격력 **30**으로 전 적 중 최고. HP 25로 매우 약한데 cost는 spitter(4)보다 겨우 1 높다. 접촉 시 34프레임(0.57초) 안에 반경 70을 벗어나야 한다. **단일 피해량 대비 cost가 가장 저렴한 적**이다. |
| **spitter** | **4** | 유일하게 "거리를 벌려도 피해를 주는" 저비용 적이다 (`4890-4899`의 220/170 거리 유지 로직). Tier 1에서 t≥200이면 전체 스폰의 절반이 spitter가 된다 (§2, §12) — 이때 화면은 **전부 투사체**가 된다. |

#### (C) HP 때문에 비용이 높은 적 vs 특수능력 때문에 비용이 높은 적

| 분류 | 적 | 판단 근거 |
|---|---|---|
| **HP 기반** | brute(7/HP60), armorwalker(7/HP95), stonegolem(12/HP170) | cost/HP 비가 0.071~0.117로 좁게 모여 있고, 셋 다 `ENEMY_ROLE`이 `tank`다. cost ≈ HP × 0.1 에 가깝다. |
| **특수능력 기반** | howler(8/HP34), necromancer(10/HP64), reaper(11/HP42) | cost/HP 비가 0.156~0.262로 tank의 2~3배. HP로는 설명되지 않는 비용이며, 각각 아군 가속·소환/부활·점멸이라는 고유 행동이 있다. |
| **불일치** | bomber(5/HP25 = 0.2) | 특수능력 기반 비율인데 cost 절대값은 spitter와 1 차이뿐이다. |

**요약:** threat cost는 대체로 `HP × 0.1 + 특수능력 가산`으로 설명되지만, **이동속도가 비용에 전혀 반영되어 있지 않다.** 속도 0.4~0.5인 tank 3종이 가장 비싸고, 속도 1.5~1.7인 runner·reaper가 실제 압박을 만든다. 이 때문에 "예산을 비싼 적으로 채우면 오히려 압박이 줄어드는" 역전이 발생한다 (§13 문제 3).

---

## 4. 역할군

`ENEMY_ROLE`(`index.html:2837-2845`)에 **7개 역할군**이 코드로 명시되어 있다. 임의로 만든 이름이 아니다.

| 역할군 | 포함 적 | 이동 방식 | 공격 방식 | 특수 행동 | 플레이어 대응 | 다른 역할군과의 차이 |
|---|---|---|---|---|---|---|
| `chaser` | walker, runner, slimeling | 직선 추격 (`4915`) | 접촉 | 없음 | 계속 움직이기 | **행동이 없다.** 유일한 변수는 속도 |
| `tank` | brute, armorwalker, stonegolem | 직선 추격, 속도 0.4~0.5 | 접촉 (+골렘 충격파) | armorwalker 방향 방어 / stonegolem 전조 충격파 | 무시하거나 측면 공략 | HP가 높다 — 그러나 **brute는 chaser와 행동이 완전히 동일**하다 |
| `ranged` | spitter, archer, lich | **거리 유지** (220 밖→접근, 170 안→후퇴, `4890`) | 투사체 | spitter 35% 2연발 / lich 시체 부활 | 투사체 회피 + 추격해서 제거 | **접촉 없이 피해를 준다** — chaser/tank와 근본적으로 다르다 |
| `area` | slime, firedemon | slime 직선 추격 / firedemon 추격+장판 | 접촉 (+장판) | slime 사망 분열 / firedemon 경로 예측 점화 | 지역 회피 | **바닥이 위험해진다** |
| `support` | howler, necromancer | 거리 유지 (howler 125~165, necro 190~240) | howler 접촉 / necro 없음 | howler 아군 ×1.4 가속 / necro 소환·부활 | **최우선 제거** | **다른 적을 강화한다** — 방치하면 다른 모든 적의 위험도가 올라간다 |
| `assassin` | reaper | 추격 + 점멸 | 접촉 | 전조 후 측면 150으로 순간이동 | 전조 위치 예측 회피 | **위치 예측이 무의미해진다** |
| `suicide` | bomber | 직선 추격, 속도 1.05 | 자폭 | 접촉 시 전조 후 폭발 | 전조 반경 이탈 | **죽여도 폭발한다** (`bomberExplode`는 `e.dead=true`를 스스로 설정) |

### 4-1. 같은 역할군인데 행동이 실질적으로 다른 적

- **`tank` 내부 편차가 크다.** `brute`는 `ENEMY_BEHAVIORS`에 항목이 없어 **`chaser`와 코드 경로가 완전히 같다**(`updateEnemies`의 최종 `else`). `armorwalker`는 피해 계산이 바뀌고, `stonegolem`은 전조 공격이 있다. 셋을 하나의 역할군으로 묶는 근거는 HP뿐이다.
- **`ranged` 내부**: `spitter`/`archer`는 `def.spit` 분기(`4890`)만 타고, `lich`는 `ENEMY_BEHAVIORS.lich`(`2910`)를 먼저 타므로 **시체가 있으면 사격 대신 부활을 한다**. 즉 lich는 실질적으로 `support`에 가깝다.
- **`area` 내부**: `slime`은 `ENEMY_BEHAVIORS`에 없어 직선 추격만 하고, 위험은 **사망 후**에 생긴다. `firedemon`은 생존 중에 계속 장판을 깐다. 발생 시점이 정반대다.

### 4-2. HP만 다른 경우를 별도 역할로 과대평가하지 않기
`walker`(HP20) ↔ `brute`(HP60)는 **코드 경로가 동일**하다. `slimeling`(HP10) ↔ `walker`(HP20)도 동일하다. 이들은 서로 다른 행동 역할이 아니라 **같은 행동의 체력 변형**이다.

---

## 5. Threat Budget 구조 — 실제 데이터 흐름

### 5-1. 호출 관계 (실제 코드 기준)

```
update()                                      index.html:4818
  └ G.time += 1/60                            index.html:4819
  └ spawnDirector()                           index.html:4851  (호출 지점)
       │
       ├─ if (--G.threatT <= 0) {             index.html:4718
       │     G.threatT = THREAT_BALANCE.window (600프레임 = 10초)
       │     refreshThreatBudget()            index.html:3081
       │       ├ b = (base + t*perSec) * (1 + (tier-1)*tierMul)
       │       ├ if (G.boss)  b *= bossCut    ← 보스 존재 시. 본 보고서 분석 범위 밖
       │       ├ else if (mini) b *= miniCut
       │       ├ G.threatBudget = b
       │       └ G.threatRoles = waveRolesFor(t).filter(k => roleUnlocked(k,t))
       │                          ↑ 3066         ↑ 3072
       │  }
       │
       └─ G.spawnTimer--                      index.html:4719
          if (G.spawnTimer <= 0 && G.enemies.length < hardCap) {   index.html:4720
              dens = 1 + (G.tier-1)*0.22                            index.html:4722
              G.spawnTimer = Math.max(6, (55 - t*0.85) / dens)      index.html:4723
              spawnThreatWave()                                     index.html:4724 → 3091
                 ├ spend = G.threatBudget * perTick(0.16)           index.html:3093
                 └ while (spend>0 && guard++<24 && G.enemies.length<hardCap)   3096
                      key  = G.threatRoles[random]                  index.html:3097
                      cost = ENEMY_THREAT_COST[key] || 2            index.html:3098
                      if (cost > spend && guard > 1) break          index.html:3099
                      spawnEnemy(key)                               index.html:3100 → 4698
                         └ if (G.enemies.length >= hardCap) return null   index.html:4699
                         └ G.enemies.push(e)                        index.html:4712
                      spend -= cost                                 index.html:3101
          }
```

지시서에 제시된 흐름도와 실제 코드는 **일치한다.** 단, 다음 두 가지가 지시서에 없던 사실이다.

1. `refreshThreatBudget()`은 **매 프레임이 아니라 600프레임(10초)마다 한 번만** 호출된다. 따라서 `G.threatBudget`은 10초 동안 **고정값**이고, 스폰은 그 구간 내내 같은 예산으로 이뤄진다.
2. hardCap 검사는 **세 곳**에 있다 — `spawnDirector`(4720), `spawnThreatWave`의 while 조건(3096), `spawnEnemy` 첫 줄(4699). 삼중 방어 구조다.

### 5-2. `THREAT_BALANCE` 실제 값 (`index.html:3047-3056`)

| 키 | 값 | 사용 위치 | 계산에서의 역할 |
|---|---|---|---|
| `window` | **600** | `spawnDirector` `4718` | `G.threatT` 리셋값. 예산 재계산 주기 = 600프레임 = **10초** |
| `base` | **6** | `refreshThreatBudget` `3083` | 예산 상수항 |
| `perSec` | **0.26** | `refreshThreatBudget` `3083` | 시간당 예산 증가 계수 |
| `tierMul` | **0.20** | `refreshThreatBudget` `3083` | `(1 + (tier-1)*0.20)` — 차수당 예산 +20% |
| `bossCut` | **0.45** | `refreshThreatBudget` `3084` | 보스 생존 시 예산 ×0.45. **스폰 계산에 들어간다는 사실만 기록**하고 보스 시스템은 분석하지 않음 |
| `miniCut` | **0.7** | `refreshThreatBudget` `3085` | 미니보스 생존 시 예산 ×0.7 (`bossCut`과 배타적 `else if`) |
| `hardCap` | **300** | `spawnDirector` `4720`, `spawnThreatWave` `3096`, `spawnEnemy` `4699` | 동시 적 객체 상한 |
| `perTick` | **0.16** | `spawnThreatWave` `3093` | 한 wave가 쓸 수 있는 예산 비율 |

예산 공식 (실제 값 대입):
```
budget(t, tier) = (6 + 0.26·t) · (1 + 0.20·(tier−1))
wave 할당량      = budget · 0.16
```

| t | Tier1 budget | 할당 | Tier2 budget | 할당 | Tier3 budget | 할당 |
|---|---|---|---|---|---|---|
| 0 | 6.0 | 0.96 | 7.2 | 1.15 | 8.4 | 1.34 |
| 30 | 13.8 | 2.21 | 16.6 | 2.65 | 19.3 | 3.09 |
| 60 | 21.6 | 3.46 | 25.9 | 4.15 | 30.2 | 4.84 |
| 120 | 37.2 | 5.95 | 44.6 | 7.14 | 52.1 | 8.33 |
| 180 | 52.8 | 8.45 | 63.4 | 10.14 | 73.9 | 11.83 |

### 5-3. `guard > 1` 의 실제 의미 (`index.html:3099`)
`guard`는 `guard++ < 24`에서 후위 증가하므로 **루프 첫 바퀴 안에서는 `guard === 1`**이다. 따라서 `cost > spend && guard > 1` 조건은 첫 선택에서 **항상 false**다 → **예산이 부족해도 wave마다 최소 1마리는 반드시 스폰된다.** 이 때문에 초반(t=0, 할당 0.96)에도 walker(cost 1)나 runner(cost 2)가 나오고 `spend`는 음수가 되며 루프가 즉시 끝난다. 시뮬레이터 §9에서 초반 예산 소진율이 **129.7%**로 100%를 넘는 이유가 이것이다.

---

## 6. Spawn Interval

### 6-1. 실제 코드 (`index.html:4722-4723`)
지시서에 제시된 형태와 **실제 코드가 정확히 일치**한다.
```js
const dens = 1 + (G.tier - 1) * 0.22;   // 차수마다 스폰 밀도 증가
G.spawnTimer = Math.max(6, (55 - t * 0.85) / dens);
```
- `G.spawnTimer`는 프레임 단위 실수이며 매 프레임 1씩 감소한다 (`4719`).
- 초기값은 `0` (`G` 선언 `4181`, `startRun` `4295`) → **첫 프레임에 즉시 첫 wave**가 발생한다.

### 6-2. Tier × 시점별 스폰 간격 (실제 숫자 계산)

| t(초) | Tier 1 | Tier 2 (dens 1.22) | Tier 3 (dens 1.44) |
|---|---|---|---|
| 0 | 55.00f (0.917s) | 45.08f (0.751s) | 38.19f (0.637s) |
| 30 | 29.50f (0.492s) | 24.18f (0.403s) | 20.49f (0.341s) |
| 60 | **6.00f (0.100s)** | **6.00f (0.100s)** | **6.00f (0.100s)** |
| 90 | 6.00f (0.100s) | 6.00f (0.100s) | 6.00f (0.100s) |
| 120 | 6.00f (0.100s) | 6.00f (0.100s) | 6.00f (0.100s) |
| 180 | 6.00f (0.100s) | 6.00f (0.100s) | 6.00f (0.100s) |

### 6-3. 최소 간격 도달 시점
`Math.max(6, …)`가 6으로 고정되는 시각은 `(55 − 6·dens) / 0.85`:

| Tier | 최소 간격 도달 시각 |
|---|---|
| 1 | **t ≥ 57.65s** |
| 2 | **t ≥ 56.09s** |
| 3 | **t ≥ 54.54s** |

→ **약 55~58초 이후에는 모든 차수의 스폰 간격이 6프레임으로 완전히 같아진다.** `dens`(차수 밀도 계수)는 **런 첫 1분에만 의미가 있고, 나머지 4분 동안은 전혀 작동하지 않는다.**

### 6-4. 실제 스폰 시도 빈도
- t=0: Tier1 약 **1.09회/초**, Tier3 약 1.57회/초
- t≥58: 전 차수 공통 **10.0회/초** (6프레임 주기)
- 시뮬레이션 누적 시도 횟수(1,000 seed 평균, §3 출력):

| t | Tier1 시도 | Tier2 시도 | Tier3 시도 |
|---|---|---|---|
| 30s | 44.0 | 53.0 | 62.0 |
| 60s | 174.0 | 206.0 | 235.0 |
| 120s | 774.0 | 806.0 | 835.0 |
| 180s | 1374.0 | 1406.0 | 1435.0 |

t=180 기준 Tier1 대비 Tier3의 시도 횟수 차이는 **겨우 +4.4%**다. 첫 60초에 벌어진 격차(+35%)가 이후 전혀 확대되지 않는다.

---

## 7. 시뮬레이션 방법

파일: **`/tmp/spawn-sim.js`** — 실행 완료, 출력 `/tmp/spawn-sim-out.txt` (319줄).

### 7-1. 원본에서 그대로 복사한 것
| 항목 | 원본 위치 |
|---|---|
| `ENEMY_THREAT_COST` | 3042-3046 (값 변경 없음) |
| `THREAT_BALANCE` | 3047-3056 (값 변경 없음) |
| `WAVE_COMPOSITION` | 3057-3065 (값 변경 없음) |
| `ENEMY_ROLE` | 2837-2845 (역할군 집계용) |
| `waveRolesFor()` | 3066-3070 (로직 동일) |
| `roleUnlocked()` | 3072-3079 (로직 동일, `G.tier` 를 인자로 전달하도록만 변경) |
| `refreshThreatBudget()` | 3081-3090 (로직 동일) |
| `spawnThreatWave()` | 3091-3104 (로직 동일. `guard > 1` 규칙, break 순서, 3중 hardCap 검사 모두 보존) |
| `spawnDirector()` 스폰 부분 | 4716-4724 (로직 동일) |
| `spawnEnemy()` 의 hardCap 검사 | 4699 |
| `G.time += 1/60` | 4819 |
| 스폰 간격 공식 | 4722-4723 |

### 7-2. RNG
`Math.random()` → **mulberry32** 시드 RNG로 대체.
- seed 수: **1,000**
- 결정성 확인: 동일 seed 재실행 시 결과 완전 일치 (`true`), 다른 seed는 다른 결과 (`true`) — 시뮬레이터 §0 출력.

### 7-3. 의도적으로 생략한 의존성
| 생략한 것 | 이유 |
|---|---|
| `spawnEnemy()`의 좌표·스탯 초기화 (4701-4711) | 스폰 **수**와 **종류**에 영향을 주지 않음 |
| `hpMul()`, `tierSpdMul()` | 적의 강도에만 영향, 스폰량에 무관 |
| 보스/미니보스 스폰 (4726-4727) | 분석 범위 밖. `bossCut`/`miniCut` 분기는 구조만 남기고 기본 시나리오에서 비활성 |
| 하트 스폰 (4728-4734) | 적이 아님 |
| 변이·네크로 소환·슬라임 분열의 추가 `spawnEnemy` 호출 (3014, 3016, 4645, 4965, 5195) | 위협 예산 시스템 외부의 스폰 경로. §13 문제 5에서 구조적 위험만 지적 |

### 7-4. 두 가지 실행 모드
- **처치 무관 모드** (`killPerSec = null`): 매 프레임 `aliveCount = 0`으로 비워 hardCap이 절대 걸리지 않게 한다 = **스폰 시스템이 의도한 출력**. §8의 주력 지표.
- **kill/s 모드**: 초당 처치 수를 파라미터로 받아 `aliveCount`를 감소시킨다. 처치는 스폰보다 **먼저** 적용한다 (원본 `update()`에서 무기 발사 `4846-4849`가 `spawnDirector()` `4851`보다 앞서기 때문).

---

## 8. 누적 스폰 결과 (처치 무관 — 주력 지표)

> 1,000 seed 평균. hardCap이 걸리지 않는 조건이므로 **스폰 시스템이 만들어내려고 한 값**이다.

### Tier 1
| t | A.누적스폰 | B.누적cost | G.시도수 | F.간격(f) | budget | C/D. 종류별 누적 비율 |
|---|---|---|---|---|---|---|
| 30 | 44.0 | 54.9 | 44.0 | 29.54 | 13.8 | walker 75.2% · runner 24.8% |
| 60 | 222.6 | 453.0 | 174.0 | 6.00 | 21.6 | walker 54.9% · runner 33.4% · brute 11.7% |
| 90 | 606.1 | 1597.8 | 474.0 | 6.00 | 29.4 | runner 38.8% · walker 37.1% · brute 14.2% · bomber 9.9% |
| 120 | 1009.1 | 2870.8 | 774.0 | 6.00 | 37.2 | runner 35.9% · walker 33.0% · brute 14.4% · bomber 11.9% · spitter 4.7% |
| 180 | 1851.4 | 5961.2 | 1374.0 | 6.00 | 50.2 | runner 32.6% · walker 24.5% · bomber 15.2% · brute 15.2% · spitter 12.5% |

**E. 역할군 비율 (Tier 1)**
| t | chaser | tank | suicide | ranged |
|---|---|---|---|---|
| 30 | 100.0% | — | — | — |
| 60 | 88.3% | 11.7% | — | — |
| 90 | 75.9% | 14.2% | 9.9% | — |
| 120 | 68.9% | 14.4% | 11.9% | 4.7% |
| 180 | **57.1%** | 15.2% | 15.2% | 12.5% |

### Tier 2
| t | A.누적스폰 | B.누적cost | G.시도수 | F.간격(f) | budget | C/D. 종류별 비율 |
|---|---|---|---|---|---|---|
| 30 | 64.8 | 78.0 | 53.0 | 24.32 | 16.6 | walker 79.7% · runner 20.3% |
| 60 | 290.4 | 567.9 | 206.0 | 6.00 | 25.9 | walker 57.0% · runner 32.5% · brute 10.5% |
| 90 | 682.1 | 1917.6 | 506.0 | 6.00 | 35.3 | runner 36.8% · walker 36.7% · brute 11.8% · armorwalker 7.3% · bomber 7.3% |
| 120 | 1084.4 | 3387.0 | 806.0 | 6.00 | 44.6 | runner 33.2% · walker 31.6% · brute 12.0% · bomber 9.7% · armorwalker 9.2% · spitter 4.2% |
| 180 | 1971.8 | 7051.5 | 1406.0 | 6.00 | 60.2 | runner 29.4% · walker 22.8% · bomber 13.0% · brute 12.7% · armorwalker 11.2% · spitter 10.9% |

**E. 역할군 비율 (Tier 2, t=180)**: chaser 52.2% · tank 23.9% · suicide 13.0% · ranged 10.9%

### Tier 3
| t | A.누적스폰 | B.누적cost | G.시도수 | F.간격(f) | budget | C/D. 종류별 비율 |
|---|---|---|---|---|---|---|
| 30 | 76.1 | 91.5 | 62.0 | 20.66 | 19.3 | walker 79.8% · runner 20.2% |
| 60 | 376.8 | 716.3 | 235.0 | 6.00 | 30.2 | walker 55.7% · runner 35.1% · brute 9.2% |
| 90 | 785.8 | 2101.8 | 535.0 | 6.00 | 41.2 | walker 38.3% · runner 37.9% · brute 10.8% · bomber 6.7% · armorwalker 6.4% |
| 120 | 1212.8 | 3675.9 | 835.0 | 6.00 | 52.1 | runner 33.9% · walker 32.3% · brute 10.7% · bomber 9.1% · armorwalker 7.8% · spitter 3.2% · slime 3.1% |
| 180 | 2143.3 | 7914.6 | 1435.0 | 6.00 | 70.3 | runner 28.3% · walker 23.2% · bomber 11.6% · brute 11.3% · armorwalker 9.7% · spitter 9.1% · slime 4.6% · reaper 2.3% |

**E. 역할군 비율 (Tier 3, t=180)**: chaser 51.5% · tank 21.0% · suicide 11.6% · ranged 9.1% · area 4.6% · assassin 2.3%

### 8-1. 초당 스폰 속도 · 초당 threat cost

| 구간 | Tier1 마리/s | Tier1 cost/s | Tier2 마리/s | Tier2 cost/s | Tier3 마리/s | Tier3 cost/s |
|---|---|---|---|---|---|---|
| 0~30s | 1.47 | 1.83 | 2.16 | 2.60 | 2.54 | 3.05 |
| 30~60s | 5.95 | 13.27 | 7.52 | 16.33 | 10.03 | 20.83 |
| 60~90s | 12.78 | 38.16 | 13.06 | 44.99 | 13.63 | 46.18 |
| 90~120s | 13.43 | 42.43 | 13.41 | 48.98 | 14.23 | 52.47 |
| 120~180s | **14.04** | 51.51 | **14.79** | 61.07 | **15.51** | 70.65 |

**초당 스폰 마릿수가 60초 이후 사실상 평평하다** (12.8 → 14.0, +10%). Tier 1과 Tier 3의 차이도 **+10.5%**뿐이다.

### 8-2. wave 당 마리 수 · 예산 소진율

| Tier | t | budget | 할당(×0.16) | 실사용 | 소진율 | 평균 마리/wave | 역할 슬롯 수 |
|---|---|---|---|---|---|---|---|
| 1 | 0 | 6.0 | 0.96 | 1.24 | 129.7% | 1.00 | 4 |
| 1 | 60 | 21.6 | 3.46 | 3.70 | 107.1% | 1.20 | 5 |
| 1 | 120 | 37.2 | 5.95 | 4.29 | **72.1%** | 1.31 | 5 |
| 1 | 180 | 52.8 | 8.45 | 5.85 | **69.2%** | 1.45 | 4 |
| 1 | 240 | 68.4 | 10.94 | 9.32 | 85.2% | **3.22** | **2** |
| 2 | 180 | 63.4 | 10.14 | 7.29 | 71.9% | 1.61 | 5 |
| 3 | 180 | 73.9 | 11.83 | 8.37 | 70.8% | 1.58 | 6 |

- **런 중반(t=90~180)에 할당 예산의 28~30%가 매 wave 버려진다.** 비싼 적을 한 마리 뽑으면 `cost > spend`로 루프가 즉시 끊기기 때문이다 (`index.html:3099`).
- wave 크기는 전 구간 **1.0~1.6마리**로 거의 늘지 않는다. 예산 증가는 "더 큰 무리"가 아니라 "더 비싼 개체 하나"로 소비된다.
- **역할 슬롯이 줄어들수록 wave가 커진다** (Tier1 t=240, 슬롯 2개 → 3.22마리/wave). 싼 적(runner 2, spitter 4)만 남아 항상 감당 가능하기 때문이다. 즉 **다양성 붕괴가 일어나는 구간에서 오히려 수가 늘어난다.**

---

## 9. kill/s 가정 기반 동시 적 수 추정

> **⚠ 이 수치는 kill/s 가정 기반 추정치다. 실제 플레이에서 측정한 값이 아니다.**
> 처치 모델: 매 프레임 `killPerSec/60`을 누적해 정수분만큼 `aliveCount`에서 제거. 1,000 seed 평균.

### 2 kill/s 가정
| Tier | t=30 | t=60 | t=90 | t=120 | t=180 | hardCap 도달 | 평균 도달 시각 |
|---|---|---|---|---|---|---|---|
| 1 | 0.0 | 118.6 | **300.0** | 300.0 | 300.0 | 1000/1000 | **77.9s** |
| 2 | 12.8 | 178.4 | **300.0** | 300.0 | 300.0 | 1000/1000 | **71.1s** |
| 3 | 19.1 | 259.8 | **300.0** | 300.0 | 300.0 | 1000/1000 | **63.6s** |

### 5 kill/s 가정
| Tier | t=30 | t=60 | t=90 | t=120 | t=180 | hardCap 도달 | 평균 도달 시각 |
|---|---|---|---|---|---|---|---|
| 1 | 0.0 | 62.1 | 293.3 | **300.0** | 300.0 | 1000/1000 | **90.4s** |
| 2 | 0.5 | 93.7 | **300.0** | 300.0 | 300.0 | 1000/1000 | **85.7s** |
| 3 | 0.1 | 153.7 | **300.0** | 300.0 | 300.0 | 1000/1000 | **77.9s** |

### 10 kill/s 가정
| Tier | t=30 | t=60 | t=90 | t=120 | t=180 | hardCap 도달 | 평균 도달 시각 |
|---|---|---|---|---|---|---|---|
| 1 | 0.0 | 20.0 | 103.5 | 206.5 | **300.0** | 1000/1000 | **142.2s** |
| 2 | 0.0 | 30.0 | 121.7 | 224.0 | **300.0** | 1000/1000 | **136.7s** |
| 3 | 0.0 | 72.9 | 181.9 | 295.4 | **300.0** | 1000/1000 | **117.3s** |

**세 가정 모두, 세 차수 모두, 1,000/1,000 seed에서 hardCap 300에 도달한다.**

---

## 10. hardCap 분석

`THREAT_BALANCE.hardCap = 300` (`index.html:3053`).
적용 위치 3곳: `spawnDirector` (4720), `spawnThreatWave` while 조건 (3096), `spawnEnemy` 첫 줄 (4699).

### 10-1. 언제 병목이 되는가

| kill/s | Tier1 도달 | Tier2 도달 | Tier3 도달 | 억제된 프레임(180초 중) |
|---|---|---|---|---|
| 2 | 77.9s | 71.1s | 63.6s | 6,127 / 6,535 / 6,986 프레임 (전체 10,800프레임의 **57~65%**) |
| 5 | 90.4s | 85.7s | 77.9s | 5,377 / 5,658 / 6,127 프레임 (**50~57%**) |
| 10 | 142.2s | 136.7s | 117.3s | 1,512 / 2,166 / 3,136 프레임 (**14~29%**) |

**핵심 질문에 대한 답:** 시뮬레이션한 세 조건(2·5·10 kill/s) **전부에서** hardCap이 병목으로 작동한다. 10 kill/s에서도 Tier1 기준 t=142s에 도달한다. 즉 **런의 후반 절반은 항상 캡에 붙어 있다**고 보아야 한다.

### 10-2. 스폰 억제량 — 의도한 출력 대비 실제 출력

| Tier | kill/s | t=180 의도 스폰 | t=180 실제 스폰 | **손실률** |
|---|---|---|---|---|
| 1 | 2 | 1851.4 | 643.0 | **65.3%** |
| 1 | 5 | 1851.4 | 1060.5 | **42.7%** |
| 1 | 10 | 1851.4 | 1701.6 | 8.1% |
| 2 | 2 | 1971.8 | 651.0 | **67.0%** |
| 2 | 5 | 1971.8 | 1096.7 | **44.4%** |
| 2 | 10 | 1971.8 | 1759.5 | 10.8% |
| 3 | 2 | 2143.3 | 656.0 | **69.4%** |
| 3 | 5 | 2143.3 | 1123.1 | **47.6%** |
| 3 | 10 | 2143.3 | 1802.9 | 15.9% |

### 10-3. threat budget이 남는데도 적을 만들지 못하는가
**그렇다.** `spawnThreatWave`의 while 조건(`index.html:3096`)에 `G.enemies.length < T.hardCap`이 들어 있어, `spend`가 아직 양수여도 캡에 걸리면 루프가 즉시 끝난다. 이때 남은 `spend`는 **다음 wave로 이월되지 않고 그냥 버려진다** (`spend`는 함수 지역 변수, `3093`). 2 kill/s Tier3 기준 전체 프레임의 65%가 이 상태다.

### 10-4. 밸런스 관점 vs 성능 관점 (구분)

**밸런스 관점 — 코드로 확인된 사실**
- `refreshThreatBudget`은 캡 상태와 무관하게 예산을 계속 키운다 (`3083`). t=180 Tier3 예산 73.9는 t=60의 30.2보다 2.4배지만, 캡에 붙어 있는 동안 **실제 적 수는 300으로 고정**된다.
- 결과적으로 **후반부 난이도 상승은 "적 수"가 아니라 `hpMul()`(`4223`)에 의한 개체 강화로만 전달된다.** 위협 예산 설계 의도("더 위험한 조합")가 캡 구간에서는 표현되지 못한다.

**성능 관점 — 구조적 위험만 기술 (확정하지 않음)**
- 동시 적 객체가 300개까지 상시 유지되는 구간이 런의 절반 이상이다.
- 코드 주석은 `hardCap: 300`을 "검증된 성능 범위"라고 명시한다 (`index.html:3053`).
- 완화 구조도 존재한다: 충돌 판정용 공간 그리드 `rebuildEnemyGrid()`(`4852`)와 `forEachNearbyEnemy()`, 그리고 `updateEnemyBehavior`의 거리 기반 스로틀링(`index.html:2942`: `if (d > 760 && (G.frame + e.id) % 4 !== 0) return false`).
- **본 분석에서는 FPS나 CPU를 실제로 측정하지 않았다.** 따라서 성능 문제가 발생한다고 결론 내리지 않는다. 존재하는 것은 "동시 객체 수가 상한에 상시 붙어 있는 구조"라는 사실뿐이다. (§14 참조)

---

## 11. 실제 적 조합

> 상상으로 만들지 않았다. 1,000 seed × 3 tier 시뮬레이션에서 **한 wave에 2마리 이상 나온 실제 조합**을 집계했다.
> 표본: Tier1 408,750회 / Tier2 466,598회 / Tier3 553,209회.

시뮬레이션에서 충분히 관찰된 조합만 선정했다. 5개다.

### 조합 1 — `runner + walker` (chaser 단독)
| 항목 | 값 |
|---|---|
| 구성 적 | runner ×1, walker ×1 |
| 등장 빈도 | **Tier1 25.6%** (104,732회) / Tier2 18.1% / Tier3 14.1% — 전 tier 1위 |
| 주 등장 시간대 | 평균 t=64.9~84.5s |
| threat cost 합계 | **3** |
| 역할군 조합 | chaser 단독 |
| 플레이어 대응 | 없음에 가깝다. 계속 걷기만 하면 된다 |

가장 흔한 조합이 **역할군 하나짜리**라는 점이 이 시스템의 실제 모습이다.

### 조합 2 — `runner + spitter` (chaser + ranged)
| 항목 | 값 |
|---|---|
| 구성 적 | runner ×1, spitter ×1 |
| 등장 빈도 | **Tier1 12.3%** (50,341회) / Tier2 9.2% / Tier3 4.7% |
| 주 등장 시간대 | 평균 t=139.8~156.5s (런 후반) |
| threat cost 합계 | **6** |
| 역할군 조합 | chaser + ranged |
| 플레이어 대응 | runner가 접근을 강요하는데 spitter는 접근하면 후퇴하며 쏜다(`4890-4893`). **이동 방향이 서로 반대로 요구된다** — 본 시뮬레이션에서 관찰된 조합 중 가장 명확한 상호작용 |

### 조합 3 — `bomber + runner` (suicide + chaser)
| 항목 | 값 |
|---|---|
| 구성 적 | bomber ×1, runner ×1 |
| 등장 빈도 | **Tier1 9.3%** (37,836회) / Tier2 7.9% / Tier3 5.7% |
| 주 등장 시간대 | 평균 t=140.4~164.9s |
| threat cost 합계 | **7** |
| 역할군 조합 | suicide + chaser |
| 플레이어 대응 | bomber 전조(34프레임) 반경 70 밖으로 빠져야 하는데, runner(속도 1.7)가 후퇴 경로를 막는다. **회피 방향이 제한된다** |

### 조합 4 — `armorwalker + runner` / `brute + runner` (tank + chaser)
| 항목 | 값 |
|---|---|
| 구성 적 | armorwalker ×1 + runner ×1 (또는 brute + runner) |
| 등장 빈도 | Tier2 3.4% (15,996회) + 3.4% (15,973회) / **Tier3 3.6% + 3.6%** |
| 주 등장 시간대 | 평균 t=160.4~170.0s |
| threat cost 합계 | **9** (해당 tier 최다 등장 조합 중 최고 비용) |
| 역할군 조합 | tank + chaser |
| 플레이어 대응 | armorwalker는 측면으로 돌아야 하고(`enemyArmorMul` `2848`), runner는 그 회전을 따라온다. 다만 **두 적 모두 속도가 0.5/1.7로 플레이어보다 느려** 실제 압박은 크지 않다 |

### 조합 5 — `bomber + spitter` (suicide + ranged)
| 항목 | 값 |
|---|---|
| 구성 적 | bomber ×1, spitter ×1 |
| 등장 빈도 | Tier2 3.4% (15,916회) / **Tier3 3.6%** (19,821회) |
| 주 등장 시간대 | 평균 t=160.4~170.0s |
| threat cost 합계 | **9** |
| 역할군 조합 | suicide + ranged |
| 플레이어 대응 | bomber 반경을 벗어나는 순간 spitter 투사체 라인에 들어갈 수 있다. **근접 회피와 원거리 회피가 동시에 요구되는 유일한 고빈도 조합** |

### 11-1. 관찰되지 않은 조합
- **support(howler/necromancer)가 포함된 조합은 Tier 1~3에서 단 한 번도 나오지 않았다.** `roleUnlocked`가 tier≥4를 요구하기 때문이다 (`index.html:3073`, `3076`).
- `assassin`(reaper)이 섞인 조합은 Tier3에서 전체의 2.3%에 불과해 상위 조합에 들지 못했다.
- `area`(slime/firedemon) 조합도 Tier3 4.6%로 상위권 밖이다.
- 억지로 5개를 채우지 않았다. 위 5개가 실제 빈도 상위에서 도출된 전부다.

---

## 12. Tier 1 / 2 / 3 비교

| 항목 | Tier 1 | Tier 2 | Tier 3 |
|---|---|---|---|
| 30초 spawn interval | 29.50f (0.492s) | 24.18f (0.403s) | 20.49f (0.341s) |
| 60초 spawn interval | **6.00f** | **6.00f** | **6.00f** |
| 120초 spawn interval | **6.00f** | **6.00f** | **6.00f** |
| 180초 spawn interval | **6.00f** | **6.00f** | **6.00f** |
| 누적 스폰 수 (t=180, 처치무관) | 1,851.4 | 1,971.8 (+6.5%) | 2,143.3 (+15.8%) |
| 누적 Threat Cost (t=180) | 5,961.2 | 7,051.5 (+18.3%) | 7,914.6 (+32.8%) |
| 역할군 구성 (t=180) | chaser 57.1% / tank 15.2% / suicide 15.2% / ranged 12.5% — **4종** | chaser 52.2% / tank 23.9% / suicide 13.0% / ranged 10.9% — **4종** | chaser 51.5% / tank 21.0% / suicide 11.6% / ranged 9.1% / area 4.6% / assassin 2.3% — **6종** |
| 적 종류 구성 (300초 내 등장 가능) | **5종** (walker, runner, brute, bomber, spitter) | **6종** (+armorwalker) | **8종** (+slime, reaper) |
| t≥200 이후 등장 가능 적 | **2종** (runner, spitter) | 3종 → t≥250엔 **2종** | 4종 → t≥250엔 **3종** |
| hardCap 병목 조건 | 2 k/s: 77.9s · 5 k/s: 90.4s · 10 k/s: 142.2s | 71.1s · 85.7s · 136.7s | 63.6s · 77.9s · 117.3s |

### 12-1. 차수 상승 시 실제로 변하는 것

코드와 시뮬레이션으로 확인한 결과, **네 가지 경로 중 두 개만 실질적으로 작동한다.**

| 경로 | 코드 | 실제 효과 |
|---|---|---|
| ① 스폰 빈도 증가 | `dens = 1 + (tier−1)*0.22` (`4722`) | **첫 55~58초에만 작동.** 이후 `Math.max(6, …)`로 전 차수 동일. 누적 시도 횟수 차이는 t=180에 겨우 +4.4% |
| ② threat budget 증가 | `(1 + (tier−1)*0.20)` (`3083`) | **작동한다.** 누적 cost Tier1→3 +32.8%. 다만 wave 크기는 1.45→1.58마리로 거의 안 늘고(§8-2), **더 비싼 적 한 마리**로 소비된다 |
| ③ 새로운 적 등장 | `roleUnlocked()` (`3072-3079`) | **가장 크게 작동한다.** 5종 → 6종 → 8종. Tier3에서 slime(area)·reaper(assassin) 두 역할군이 새로 열린다 |
| ④ 적 구성 변화 | ②+③의 결과 | chaser 비중 57.1% → 51.5%로 5.6%p 감소. 역할군 종류는 4 → 6 |

**결론: Tier 상승의 실질은 "새 적 종류 해금"(③) + "예산 증가에 따른 비싼 적 비중"(②)이다. 스폰 속도(①)는 첫 1분에만 기여하고 그 뒤로는 차수 간 차이가 없다.**

또한 적 개체 자체의 강화 — `tierHpMul = 1 + (tier−1)*0.60` (`4220`), `tierDmgMul = 1 + (tier−1)*0.35` (`4221`), `tierSpdMul = min(1.6, 1 + (tier−1)*0.06)` (`4222`) — 는 스폰 시스템 밖에서 따로 걸린다. Tier3의 적은 HP 2.2배·공격력 1.7배다. **hardCap으로 수가 고정되는 구간에서는 이쪽이 사실상 유일한 난이도 상승 경로다.**

---

## 13. 문제점 (5개)

### 문제 1 — 스폰 간격이 58초에 바닥을 쳐서 차수 밀도 계수가 무력화된다

**근거 코드**
```js
// index.html:4722-4723
const dens = 1 + (G.tier - 1) * 0.22;
G.spawnTimer = Math.max(6, (55 - t * 0.85) / dens);
```
`(55 − 6·dens)/0.85` → Tier1 57.65s, Tier2 56.09s, Tier3 54.54s에 6프레임 고정.

**시뮬레이션 근거**
- §6-2: t=60 이후 세 차수 모두 간격 6.00f로 동일.
- §6-4: t=180 누적 시도 횟수 Tier1 1374.0 vs Tier3 1435.0 — **차이 +4.4%**.
- §8-1: 60초 이후 초당 스폰 마릿수 12.8 → 14.0으로 사실상 평평.

**실제 게임에 미치는 영향**
5분 런 중 **4분 동안 차수와 무관하게 초당 10회 스폰 시도**가 일어난다. `dens`는 이름 그대로 "차수마다 스폰 밀도 증가"를 의도했지만 런의 19%에만 적용된다.

**심각도: 높음** — 차수 설계 의도가 런의 대부분에서 무효화된다.

**수정 방향**
`Math.max(6, …)`의 하한을 `dens`로 나눈 값(예: `Math.max(6/dens, …)`)으로 바꾸거나, 감소 계수 `0.85`를 `dens`에 연동해 바닥 도달 시점 자체를 차수별로 다르게 만든다. 단 하한을 낮추면 초당 스폰 시도가 늘어 §문제 4와 결합되므로 hardCap 정책과 함께 조정해야 한다.

---

### 문제 2 — `archer`는 완전한 정의를 갖고 있으나 어떤 경로로도 스폰되지 않는다

**근거 코드**
- 정의 존재: `ENEMIES.archer` (`index.html:2465`) — HP 24, 공격력 12, 투사체 색상까지 지정
- 비용 존재: `ENEMY_THREAT_COST.archer = 4` (`3043`)
- 역할 존재: `ENEMY_ROLE.archer = 'ranged'` (`2840`)
- 해금 조건 존재: `roleUnlocked`에 `if (key === 'archer') return t >= 210;` (`3074`)
- 스프라이트 존재: `SPRITES.archer` (`4113`)
- **그러나 `WAVE_COMPOSITION`(3057-3065) 어느 배열에도 `'archer'`가 없다.** `roleUnlocked`는 `waveRolesFor(t)`가 반환한 배열을 필터링할 뿐이므로(`3088`), 목록에 없는 키는 평가조차 되지 않는다.
- archer를 실제로 뽑던 코드는 `rollEnemyType()`의 `if (t >= 210) extras.push('archer')` (`4748`)인데, **`rollEnemyType`은 파일 전체에서 정의(4736) 외에 호출 지점이 0곳이다.**
- 도감은 플레이어에게 "3분 30초 이후 등장"이라고 안내한다 (`3473`, `3480`).

**시뮬레이션 근거**
§8 출력: `ENEMY_THREAT_COST 에만 있고 WAVE_COMPOSITION 에 없는 적: slimeling, archer`. Tier 1~5 전 구간 300초 시뮬레이션에서 archer 스폰 **0회**.

**실제 게임에 미치는 영향**
적 15종 중 1종이 사용되지 않는다. 도감이 거짓 정보를 표시한다. `ranged` 역할군은 실질적으로 spitter(전 차수) + lich(tier≥5) 2종뿐이다.

**심각도: 중간** — 밸런스 붕괴는 없으나 콘텐츠 손실 + 플레이어에게 잘못된 안내.

**수정 방향**
`WAVE_COMPOSITION`의 `from:200` / `from:250` 항목에 `'archer'`를 추가하면 기존 `roleUnlocked`의 `t >= 210` 조건이 그대로 살아난다. 또는 도감 문구를 실제 동작에 맞춘다. 함께 `rollEnemyType()`(4736-4754, 19줄) 제거 여부도 검토 대상이다.

---

### 문제 3 — Tier 1~2 후반에 적 종류가 2종으로 붕괴한다

**근거 코드**
```js
// index.html:3063-3064
{ from:200, roles:['runner','spitter','howler','reaper','necromancer','stonegolem','armorwalker'] },
{ from:250, roles:['runner','reaper','howler','necromancer','stonegolem','firedemon','lich','spitter'] },
```
`roleUnlocked`(3072-3079)가 howler(tier≥4), reaper(tier≥3), necromancer·stonegolem(tier≥4), armorwalker(tier≥2), firedemon·lich(tier≥5)를 걸러낸다.

**시뮬레이션 근거** (§2 출력)
| Tier | t≥150 | t≥200 | t≥250 |
|---|---|---|---|
| 1 | 4종 | **2종** [runner, spitter] | **2종** |
| 2 | 5종 | 3종 [runner, spitter, armorwalker] | **2종** [runner, spitter] |
| 3 | 6종 | 4종 | 3종 |

또한 §8-2: Tier1 t=240에서 역할 슬롯이 2개가 되자 wave 크기가 1.45 → **3.22마리**로 뛴다. 싼 적만 남아 예산을 전부 소진할 수 있게 되기 때문이다.

**실제 게임에 미치는 영향**
런은 300초에 최종 보스로 끝난다 (`spawnDirector` `4727`). 즉 **Tier 1 플레이어는 런의 마지막 100초 동안 runner와 spitter 두 종류만 본다.** 신규 플레이어가 가장 먼저 겪는 차수에서 후반 다양성이 가장 낮다. 또한 `WAVE_COMPOSITION`의 중복 가중치(예: `from:0`의 walker ×3)가 필터링 후 사라져 **의도한 출현 비율이 조용히 재분배된다** — t≥200 Tier1에서 runner는 설계상 1/7(14%)이어야 하는데 실제로는 1/2(50%)가 된다.

**심각도: 높음** — 가장 많은 플레이어가 겪는 구간의 콘텐츠가 가장 빈약하다.

**수정 방향**
`from:200` / `from:250` 배열에 저차수에서도 해금되는 적(walker, brute, bomber)을 남겨 두어 필터링 후에도 최소 4~5종이 유지되게 한다. 또는 `refreshThreatBudget`의 폴백(`3089`: `if (!G.threatRoles.length) G.threatRoles = ['walker','runner']`)을 "N종 미만이면 보충"으로 확장한다.

---

### 문제 4 — hardCap 300이 모든 kill/s 조건에서 병목으로 작동하며, 남은 예산은 버려진다

**근거 코드**
```js
// index.html:3096  (spawnThreatWave)
while (spend > 0 && guard++ < 24 && G.enemies.length < T.hardCap) {
```
`spend`는 함수 지역 변수(`3093`)이므로 캡에 막혀 남은 예산은 다음 wave로 **이월되지 않는다**. `refreshThreatBudget`(3081)은 캡 상태를 전혀 참조하지 않고 예산을 계속 키운다.

**시뮬레이션 근거** (§9, §10)
- 2 / 5 / 10 kill/s × Tier 1/2/3 = 9개 조건 **전부에서 1000/1000 seed가 hardCap 도달**.
- 도달 시각: 최속 63.6s (Tier3, 2 k/s) ~ 최지연 142.2s (Tier1, 10 k/s).
- 180초 중 캡 상태 프레임 비율: 2 k/s에서 **57~65%**, 10 k/s에서도 14~29%.
- 의도 스폰 대비 손실률: 2 k/s에서 **65~69%**, 5 k/s에서 **43~48%**.

**실제 게임에 미치는 영향**
- **밸런스**: 위협 예산 시스템이 "더 위험한 조합"을 만들려고 계산한 예산의 최대 2/3가 실제 개체로 표현되지 못한다. 캡 구간에서 차수 간 난이도 차이는 스폰이 아니라 `tierHpMul`(4220)·`tierDmgMul`(4221)로만 전달된다.
- **성능(구조적 위험만 기술)**: 동시 적 객체가 300개인 상태가 런의 절반 이상 지속된다. 코드에는 공간 그리드(`rebuildEnemyGrid` 4852)와 거리 기반 행동 스로틀링(`updateEnemyBehavior` 2942)이라는 완화 장치가 있다. **본 분석은 FPS/CPU를 측정하지 않았으므로 성능 문제를 확정하지 않는다.**

**심각도: 높음 (밸런스)** / **미확정 (성능)**

**수정 방향**
(a) 캡에 걸렸을 때 남은 `spend`를 이월하거나, (b) `refreshThreatBudget`에서 캡 근접 시 예산을 억제하고 대신 `hpMul` 쪽으로 난이도를 옮기거나, (c) 예산을 "마릿수"가 아니라 "개체 강화"로도 지출할 수 있게 한다. 어느 쪽이든 실제 프레임 측정이 선행되어야 한다 (§14).

---

### 문제 5 — 한 wave의 예산 소진율이 중반에 70%로 떨어져, 예산 증가가 wave 크기로 이어지지 않는다

**근거 코드**
```js
// index.html:3099
if (cost > spend && guard > 1) break;
```
비싼 적을 한 번 뽑으면 `spend`가 크게 줄고, 다음 선택이 그보다 비싸면 **재시도 없이 즉시 루프를 종료**한다. 더 싼 후보가 배열에 남아 있어도 다시 뽑지 않는다.

**시뮬레이션 근거** (§8-2, 각 조건 10,000 wave)
| Tier | t | 할당 | 실사용 | 소진율 | 평균 마리/wave |
|---|---|---|---|---|---|
| 1 | 60 | 3.46 | 3.70 | 107.1% | 1.20 |
| 1 | 120 | 5.95 | 4.29 | **72.1%** | 1.31 |
| 1 | 180 | 8.45 | 5.85 | **69.2%** | 1.45 |
| 2 | 180 | 10.14 | 7.29 | 71.9% | 1.61 |
| 3 | 180 | 11.83 | 8.37 | 70.8% | 1.58 |

**실제 게임에 미치는 영향**
`THREAT_BALANCE.perTick = 0.16`으로 계산한 wave 예산의 약 30%가 매번 버려진다. 예산이 t=0의 6에서 t=180의 52.8로 **8.8배** 늘어도 wave 크기는 1.00 → 1.45마리(**+45%**)에 그친다. "예산이 커지면 한 번에 더 많거나 더 위험한 무리가 온다"는 설계 의도가 **한 wave 안에서는 거의 표현되지 않고**, 실제 적 증가는 §문제 1의 스폰 간격 단축이 거의 전부를 담당한다.

**심각도: 중간** — 시스템이 동작은 하나, 설계된 표현력이 나오지 않는다.

**수정 방향**
`break` 대신 "예산 내에서 고를 수 있는 후보만으로 재추첨"하고 후보가 없을 때만 종료하도록 바꾸면 소진율이 올라간다. 단 이는 wave 크기를 키워 hardCap 도달을 앞당기므로 §문제 4와 함께 조정해야 한다.

---

## 14. 분석 미확인 영역

### 14-1. 실제 플레이어 kill/s
- **무엇을 확인하지 못했나**: 실제 플레이에서 초당 몇 마리를 처치하는지. §9·§10의 모든 동시 적 수와 hardCap 도달 시각은 2·5·10 kill/s **가정**에 기반한 추정치다.
- **왜 확인하지 못했나**: kill/s는 무기 조합·레벨·패시브·시너지·대원 특성에 의해 결정되는데, 이번 작업 범위에서 무기/패시브/대원 분석이 제외되었고 실제 브라우저 플레이도 수행하지 않았다.
- **어떻게 검증할 수 있나**: `?debug` 파라미터로 게임을 띄운 뒤(`index.html:2947` 부근의 `attachDebug`) 여러 빌드로 실제 플레이하며 `G.kills`를 시간에 따라 샘플링해 구간별 kill/s 분포를 얻는다. 그 값을 `/tmp/spawn-sim.js`의 `KILLRATES`에 넣으면 추정치가 실측 기반으로 바뀐다.

### 14-2. 실제 프레임 성능 (FPS / CPU)
- **무엇을 확인하지 못했나**: 동시 적 300마리 상태에서의 실제 프레임 시간. §10-4에서 성능 문제를 **확정하지 않은** 이유다.
- **왜 확인하지 못했나**: 이번 작업은 정적 코드 분석 + 순수 JS 시뮬레이션으로 한정되었고, 렌더러(three.js InstancedMesh)·파티클·충돌 판정을 포함한 실행 환경 측정을 하지 않았다.
- **어떻게 검증할 수 있나**: 브라우저에서 `PERF` 오버레이(`index.html:4175`, F키 토글)를 켜고 `G.enemies.length`가 300에 도달한 상태의 `PERF.fps` / `PERF.ft` / `PERF.cps`를 기록한다. 실기기(모바일 포함)에서 측정해야 의미가 있다. 헤드리스 SwiftShader 측정치는 실제 GPU 성능을 대표하지 않는다.

### 14-3. 위협 예산 밖의 스폰 경로가 hardCap에 미치는 영향
- **무엇을 확인하지 못했나**: `necroSummon`(`5192`), 슬라임 분열(`4645`), 변이 사망 반응(`3014`, `3016`), 존 소환(`4965`), 미니보스 소환(`5195`)이 동시 적 수를 얼마나 밀어 올리는지.
- **왜 확인하지 못했나**: 이들은 위협 예산을 **소비하지 않고** 적을 늘리는 경로이며, 변이·보스 시스템은 이번 분석 범위에서 제외되었다. 시뮬레이터에도 재현하지 않았다.
- **어떻게 검증할 수 있나**: 해당 경로들에도 처치 모델을 붙여 시뮬레이터를 확장하거나, 브라우저에서 Tier 4~5 실플레이 중 `G.enemies.length`를 기록해 예산 기반 스폰만의 추정치와 비교한다. 참고로 `necroSummon`과 `raiseDead`에는 자체 상한 `G.enemies.length > 240`이 있다(`5194`, `2925`).

### 14-4. 원본과 시뮬레이션의 차이
- **무엇을 확인하지 못했나**: 시뮬레이터가 원본과 프레임 단위로 100% 동일한지.
- **왜 확인하지 못했나**: 재현한 것은 `spawnDirector` → `spawnThreatWave` → `spawnEnemy`(hardCap 검사만) 경로이며, 좌표 계산·객체 풀(`takeEnemy`)·스탯 초기화·`G.clearing` 분기(`4717`)·부동소수점 누적 오차는 재현하지 않았다. 특히 `G.time += 1/60`의 부동소수점 누적은 원본과 시뮬레이터가 같은 방식이지만, 실제 게임은 `loop()`의 고정 스텝 루프에서 프레임 드롭 시 최대 5스텝까지만 따라잡으므로(`while (acc >= STEP && guard < 5)`) **저사양 기기에서는 `G.time`이 실시간보다 느리게 흐를 수 있다.**
- **어떻게 검증할 수 있나**: `?debug`로 실행해 `G.time`, `G.spawnTimer`, `G.threatBudget`, `G.threatRoles`, `G.enemies.length`를 매초 로깅한 뒤 시뮬레이터의 동일 시점 값과 대조한다.

### 14-5. threat cost가 "적정"한지에 대한 최종 판단
- **무엇을 확인하지 못했나**: §3-1에서 cost와 실제 위험도의 불일치를 코드 근거로 지적했으나, "brute의 적정 cost가 4인지 5인지"는 판단하지 않았다.
- **왜 확인하지 못했나**: 적정 cost는 플레이어가 그 적을 처리하는 데 드는 실제 비용(시간·피해·이동 제약)에서 나오는데, 이는 무기 밸런스와 결합되어 결정되며 무기 분석은 이번 범위에서 제외되었다.
- **어떻게 검증할 수 있나**: 적 종류별 "평균 생존 시간"과 "플레이어가 입은 피해"를 실플레이에서 측정한다. 게임에는 이미 피해 귀속 집계(`STAT`, `recordDamage`)가 있으므로, 적 종류별 처치 소요 시간을 추가로 기록하면 cost 재산정 근거가 된다.

### 14-6. `bossCut` / `miniCut` 적용 구간의 실제 스폰
- **무엇을 확인하지 못했나**: 미니보스(t=150)와 최종 보스(t=300) 생존 중 예산이 각각 ×0.7, ×0.45로 줄어드는 구간의 실제 스폰량.
- **왜 확인하지 못했나**: 보스 시스템 분석이 범위에서 제외되어 시뮬레이터에서 `bossAlive`/`miniAlive`를 항상 false로 두었다. 따라서 **§8의 t=150~180 수치는 미니보스가 살아 있는 실제 플레이보다 최대 43% 과대평가**일 수 있다 (1/0.7 = 1.43).
- **어떻게 검증할 수 있나**: 시뮬레이터의 `G.miniAlive`를 t=150 이후 일정 구간 true로 설정해 재실행하면 즉시 비교 가능하다(코드에 스위치가 이미 있다). 단 미니보스 생존 시간은 플레이어 DPS에 달려 있어 §14-1과 같은 한계를 갖는다.

### 14-7. `roleUnlocked`의 `archer` 조건이 의도된 것인지
- **무엇을 확인하지 못했나**: archer가 `WAVE_COMPOSITION`에서 빠진 것이 의도적 제거인지 리팩터링 누락인지.
- **왜 확인하지 못했나**: 커밋 이력이나 설계 문서를 이번 작업 범위에서 조사하지 않았고, 코드만으로는 의도를 알 수 없다.
- **어떻게 검증할 수 있나**: `git log -S "archer" -- my-first-code/index.html`로 archer가 `WAVE_COMPOSITION`에 들어간 적이 있는지, 언제 빠졌는지 추적한다. 도감 문구(`3473`)가 남아 있다는 점은 누락 쪽을 시사하지만 확정 근거는 아니다.

---

## 15. 증거 기반 자체 검증

### 검증 1 — `ENEMY_THREAT_COST`가 실제 스폰 선택에 사용되는가?
**확인됨.** `spawnThreatWave()` 내부, `my-first-code/index.html:3098`:
```js
const cost = ENEMY_THREAT_COST[key] || 2;
```
이 값이 바로 다음 줄 `3099`의 `if (cost > spend && guard > 1) break;`에서 wave 종료 판정에 쓰이고, `3101`의 `spend -= cost;`에서 예산을 차감한다. 파일 전체에서 `ENEMY_THREAT_COST`가 읽히는 곳은 이 한 곳뿐이다(정의 `3042` 외).

### 검증 2 — `WAVE_COMPOSITION` → `G.threatRoles` 연결을 확인했는가?
**확인됨.** 3단계로 이어진다.
1. `waveRolesFor(t)` (`3066-3070`): `WAVE_COMPOSITION`을 순회하며 `t >= c.from`인 마지막 항목의 `roles` 배열을 반환.
2. `refreshThreatBudget()` (`3088`): `G.threatRoles = waveRolesFor(t).filter(k => roleUnlocked(k, t));`
3. `spawnThreatWave()` (`3095`, `3097`): `const roles = G.threatRoles;` → `const key = roles[(Math.random() * roles.length) | 0];`

즉 `WAVE_COMPOSITION` → `waveRolesFor` → `roleUnlocked` 필터 → `G.threatRoles` → 균등 무작위 추첨 → `key`. 시뮬레이터 §2가 이 체인의 출력을 tier×시각별로 전부 나열한다.

### 검증 3 — `THREAT_BALANCE`는 어디서 계산되는가?
**확인됨.** 정의 `3047-3056`. 사용처:
- `refreshThreatBudget()` `3082-3085`: `window`를 제외한 `base`/`perSec`/`tierMul`/`bossCut`/`miniCut` 전부
- `spawnThreatWave()` `3093`: `perTick`, `3096`: `hardCap`
- `spawnDirector()` `4718`: `window`, `4720`: `hardCap`
- `spawnEnemy()` `4699`: `hardCap`

실제 값과 각 값의 역할은 §5-2 표에 전부 기록했다.

### 검증 4 — Spawn interval은 어디서 계산되는가?
**확인됨.** `spawnDirector()` 내부, `4722-4723`:
```js
const dens = 1 + (G.tier - 1) * 0.22;
G.spawnTimer = Math.max(6, (55 - t * 0.85) / dens);
```
감소는 `4719`의 `G.spawnTimer--` (매 프레임 1회). 초기값 `0`은 `G` 선언 `4181`과 `startRun()` `4295`. 파일 전체에서 `G.spawnTimer`에 쓰는 곳은 이 세 군데뿐이다(그 외 `continueTier` `7187`, `resumeRun` `7269`도 0으로 초기화).

### 검증 5 — `G.enemies.length`는 어디서 spawn을 제한하는가?
**확인됨. 세 곳.**
1. `spawnDirector()` `4720`: `if (G.spawnTimer <= 0 && G.enemies.length < THREAT_BALANCE.hardCap)` — wave 자체를 건너뜀
2. `spawnThreatWave()` `3096`: `while (… && G.enemies.length < T.hardCap)` — wave 도중 중단
3. `spawnEnemy()` `4699`: `if (G.enemies.length >= THREAT_BALANCE.hardCap) return null;` — 개별 스폰 거부

부가 제한 2곳: `necroSummon` `5194` (`> 240`), `raiseDead` `2925` (`> 240`), 슬라임 분열 `4644` (`< 260`).

### 검증 6 — hardCap은 실제로 어떤 함수에서 적용되는가?
**확인됨.** 위 검증 5의 세 함수 — `spawnDirector`(4720), `spawnThreatWave`(3096), `spawnEnemy`(4699). 값은 `THREAT_BALANCE.hardCap = 300` (`3053`). §10에 조건별 도달 시각·억제량을 기록했고, **kill/s 가정 기반임을 §9 제목과 각 표에 명시**했다.

### 검증 7 — 시뮬레이션이 실제 원본 로직을 어느 정도 재현하는가?
**부분 재현.** 복사한 것과 생략한 것을 §7-1·§7-3에 전부 나열했다.
- **복사**: `ENEMY_THREAT_COST`, `THREAT_BALANCE`, `WAVE_COMPOSITION`, `ENEMY_ROLE` (값 변경 없음) / `waveRolesFor`, `roleUnlocked`, `refreshThreatBudget`, `spawnThreatWave`, `spawnDirector`의 스폰 부분, `spawnEnemy`의 hardCap 검사, 스폰 간격 공식, `G.time += 1/60` (로직 동일)
- **보존한 미묘한 동작**: `guard++` 후위 증가로 인한 "첫 선택은 항상 성공" 규칙(§5-3), 3중 hardCap 검사, 예산 이월 없음, 10초 주기 예산 갱신
- **생략**: 좌표/스탯 초기화, 객체 풀, `hpMul`/`tierSpdMul`, 보스·하트 스폰, 예산 외 스폰 경로(변이·소환·분열), `G.clearing` 분기, 렌더링
- **결정성**: 동일 seed 재실행 결과 완전 일치 확인 (시뮬레이터 §0 출력 `true`)
- 남은 차이와 그 영향은 §14-4·§14-6에 기록했다.

### 검증 8 — 적 조합 5개가 실제 시뮬레이션에서 도출되었는가?
**확인됨.** §11의 5개 조합은 전부 1,000 seed 시뮬레이션의 wave 로그에서 집계한 것이다. 시뮬레이터는 각 wave의 적 키를 정렬해 서명(`runner+walker` 등)으로 만들고 `Map`에 빈도·평균 시각·cost 합계를 누적한다(`aggregate()`의 `waveCounter`). 출력 §6에 tier별 빈도 상위 8개, 역할군 혼합 상위 6개, cost 상위 4개가 그대로 인쇄되어 있으며, 본문 수치(예: Tier1 `runner+walker` 25.6% / 104,732회)는 그 출력에서 직접 옮긴 값이다. **관찰되지 않은 조합(support 포함 조합 등)은 §11-1에 별도로 기록하고 억지로 채우지 않았다.**

---

## 부록 A — 안전 확인 (게임 코드 무변경)

```
$ git status --short
?? REPORT-02A-ENEMY-SPAWN.md          ← 이번에 생성한 보고서 1개뿐

$ git rev-parse origin/main:my-first-code/index.html
3a9a6d26956dc9ad6c1ede566506535e68640b93      ← 분석 시작 시점과 동일

$ git hash-object index.html
3a9a6d26956dc9ad6c1ede566506535e68640b93      ← 작업 브랜치 사본도 동일
```

- `my-first-code/index.html` **수정 없음** (blob 해시 불변)
- 작업 브랜치 루트 `index.html` **수정 없음**
- 저장소 내 신규 파일: `REPORT-02A-ENEMY-SPAWN.md` **1개뿐**
- 분석용 파일은 전부 `/tmp/` 아래: `/tmp/spawn-sim.js`, `/tmp/spawn-sim-out.txt`, `/tmp/target.html`(읽기 전용 추출본), `/tmp/game.txt`(검색용 사본)

## 부록 B — 재현 방법

```bash
# 분석 대상을 읽기 전용으로 추출
git show origin/main:my-first-code/index.html > /tmp/target.html

# 시뮬레이션 실행 (Node.js, 외부 의존성 없음)
node /tmp/spawn-sim.js

# 출력 섹션
#  0. RNG 결정성 확인          5. hardCap 병목 (의도 대비 실제)
#  1. 스폰 간격 표             6. 실제 적 조합 빈도
#  2. G.threatRoles 전개       7. wave 당 마리 수 분포
#  3. 처치 무관 누적 지표      8. 도달 가능한 적 목록
#  4. kill/s 동시 적 수 추정   9. 예산 소진율 / 10. 초당 스폰 속도
```
