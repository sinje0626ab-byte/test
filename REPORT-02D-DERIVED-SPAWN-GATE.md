# REPORT-02D — 파생 스폰 게이트(`G.popN > 240`) 검증

검증 대상: `my-first-code/index.html` (= 작업 브랜치 루트 `index.html`, 커밋 `1827278` — 스폰 타이머 수정 반영본)
시뮬레이터: `/tmp/derived-sim.js` (출력 `/tmp/dsim.txt`) — 400 seed × Tier 1~5 × 2·5·10 kill/s × 300초 × 게이트 3종
브라우저 실측: `derived.js` (출력 `/tmp/derived2.json`)
**게임 코드는 수정하지 않았다.** `git status` 기준 `index.html` 변경 0줄.

---

## 0. 코드 분석 — 파생 스폰 전수 추적

### 0-1. 파생 스폰 경로는 5개다

| # | 경로 | 호출 위치 | 게이트 | 주기 / 확률 | 1회 생성 |
|---|---|---|---|---|---|
| 1 | **네크로맨서 소환** | `updateEnemies()` → `e.def.summon` 분기 → `necroSummon(e)` | `G.popN > SPAWN_CAP.derived` (**240**) | 최초 `randInt(160,300)`f, 이후 `randInt(280,400)`f (평균 5.7초) | walker ×2 |
| 2 | **시체 부활** | `updateEnemyBehavior()` → `ENEMY_BEHAVIORS.necromancer/lich` → `raiseDead(e)` | `!G.corpses.length \|\| G.popN > 240` | 최초 `randInt(90,200)`f, 이후 `ENEMY_BAL.necro.cd = [270,360]`f (평균 5.3초) | 전조 존 최대 2개 → 각 1마리 |
| 3 | **슬라임 분열** | `killEnemy()` 인라인 | `G.popN < SPAWN_CAP.derived` (**240**) | 슬라임 사망 시 100% | slimeling ×2 |
| 4 | **변이 — 블러드문 / 무덤기상** | `killEnemy()` → `applyMutationOnKill(e)` | `G.popN < MUTATION_BALANCE[m].cap` (**280**) | 사망 시 13% / 9% | runner ×1 / 같은 종류 ×1 |
| 5 | **전조 존 소환** | `resolveZone()` → `z.spawn` | `addZone()`의 `G.zones.length > 70` + `spawnEnemy()`의 300 | 경로 2가 만든 존이 `warn=62`f 뒤 해소 | 1마리 |

### 0-2. 모든 파생 스폰은 최종적으로 `spawnEnemy()`를 지난다 — 확인됨

```js
function spawnEnemy(typeKey, ox, oy) {
  if (G.popN >= SPAWN_CAP.normal) return null;   // 300
  ...
  G.enemies.push(e); G.popN++; return e;
}
```
`spawnEnemy()` 호출 지점 전수(디버그 헬퍼 제외): 변이 2곳, 웨이브 2곳, 슬라임 분열 1곳, 전조 존 1곳, 네크로 소환 1곳. **예외 경로 없음.** 보스만 `spawnBoss()`로 따로 들어와 상한에 걸리지 않는다(`G.bossN++`).

### 0-3. 파생 스폰은 Threat Budget을 쓰지 않는다 — 확인됨

`G.threatBudget` / `G.threatCarry`를 읽거나 쓰는 곳은 `refreshThreatBudget()`과 `spawnThreatWave()` **두 곳뿐**이다. 파생 스폰은 예산을 소비하지도, 이월에 영향을 주지도 않는다. 즉 **위협 예산과 개체 상한이 서로 독립적인 두 개의 제어 장치**다.

다만 파생 스폰이 개체 슬롯을 먹으면 일반 웨이브 스폰이 상한에 더 빨리 막히므로, **개체 수를 통해 간접적으로 경쟁**한다 (§4-3의 수치 참조).

### 0-4. 파생 스폰이 갖는 별도 안전장치

| 장치 | 값 | 위치 |
|---|---|---|
| 파생 게이트 | `G.popN > 240` (분열은 `< 240`) | `necroSummon` / `raiseDead` / `killEnemy` |
| 변이 게이트 | `G.popN < 280` | `applyMutationOnKill` |
| 시체 수명 | 480프레임(8초), 최대 40구, **부활 시 목록에서 즉시 제거**(무한 부활 금지) | `addCorpse` / `raiseDead` |
| 시체 생성 제외 | 보스·미니·child·**elite** 는 시체를 남기지 않는다 | `addCorpse` |
| 부활 사거리 | 260 | `raiseDead` |
| 존 개수 상한 | 70 | `addZone` |
| 최종 상한 | 300 | `spawnEnemy` |

`elite:true`인 적은 brute · bomber · spitter · howler · armorwalker · reaper · necromancer · stonegolem · lich · firedemon **10종**. 시체를 남기는 적은 walker · runner · archer · slime · slimeling **5종뿐**이다. Tier가 올라가 elite 비중이 커지면 **부활 재료 자체가 줄어든다**.

### 0-5. "왜 240인가" — 코드·이력에서 찾은 근거

**결론부터: 240이라는 값에 대한 설계 근거는 코드에도 커밋 이력에도 남아 있지 않다.**

| 커밋 | 날짜 | 당시 상한 값 |
|---|---|---|
| `5b1782e` (최초) | 2026-07-08 | 일반 스폰 `G.enemies.length < 300` — **주석 없음** |
| `2add48f` | 2026-07-23 | `< 300` 유지 + 분열 `< 260` + 네크로 소환 `> 240` **신규, 주석 없음** |
| `670cb13` (PHASE E+G) | 2026-09-23 | `raiseDead`에 같은 `> 240` 추가 |
| `8d91b59` (PHASE H-2) | 2026-09-23 | `SPAWN_CAP.derived = 240`으로 통합, 세는 대상을 `G.enemies.length` → `G.popN`으로 변경 |

- `2add48f`의 커밋 메시지는 몬스터 8종 추가만 설명하고 240/260에 대한 언급이 없다.
- 유일하게 남은 설명은 코드 주석이며, 그것도 **PHASE H-2에서 내가 붙인 것**이다:
  - `normal: 300` — "보스/미니보스를 제외한 적 수 상한", `spawnEnemy`에 "검증된 성능 범위를 넘기지 않는다"
  - `derived: 240` — "파생 스폰(소환·부활·분열)은 이보다 붐비면 스스로 멈춘다"
- 300이라는 값 역시 최초 커밋부터 존재했고, 실제 성능 측정을 근거로 제시한 커밋은 없다.

**따라서 240은 "300에서 60만큼의 여유를 남긴다"는 헤드룸 설계로 읽히지만, 그 60이 어디서 왔는지는 확인할 수 없다.** 임의로 의도를 지어내지 않고 사실만 기록한다.

### 0-6. 코드 읽으면서 발견한 부수 사실 2가지

**(a) 게이트는 쿨다운을 소모한 *뒤*에 검사한다 — 시도가 그냥 버려진다**
```js
function raiseDead(e) {
  if (--e.spT > 0) return false;
  e.spT = randInt(B.cd[0], B.cd[1]);                    // ← 쿨다운 먼저 재설정
  if (!G.corpses.length || G.popN > SPAWN_CAP.derived) return false;   // ← 그 다음 게이트
```
막히면 다음 기회까지 다시 4.5~6초를 기다린다. "붐빌 때 잠시 미뤘다가 여유가 생기면 바로 한다"가 아니라 **그 회차를 통째로 날린다.**

**(b) 네크로맨서 소환은 연출이 먼저, 게이트가 나중이다**
```js
function necroSummon(e) {
  ring(e.x, e.y, 44, '#9a4fff', 28); if (Audio.S.howl) Audio.S.howl();   // ← 보라색 링 + 소리
  if (G.popN > SPAWN_CAP.derived) return;                                 // ← 그 다음 게이트
```
**플레이어는 소환 연출과 소리를 보고 듣지만 아무것도 나오지 않는다.** 통계가 아니라 화면에 드러나는 증상이다.

**(c) PHASE H-2에서 슬라임 분열 게이트가 260 → 240으로 조여졌다**
`8d91b59`에서 `G.enemies.length < 260`을 `G.popN < SPAWN_CAP.derived`(240)로 통합하면서 값이 20 낮아졌다. 통합 자체가 목적이었고 밸런스 변경 의도는 없었으나, 결과적으로 분열이 더 자주 막힌다. **내가 만든 부작용이므로 명시해 둔다.**

---

## 1. 설계상 의도된 파생 스폰 빈도 (질문 A)

코드 상수에서 직접 계산한 값이다.

| 행동 | 상수 | 개체 1마리당 빈도 | 1회 산출 |
|---|---|---|---|
| 네크로맨서 소환 | 최초 `randInt(160,300)`f / 재설정 `randInt(280,400)`f | 평균 **5.7초마다 1회** = 10.6회/분 | walker ×2 |
| 시체 부활 (네크로 + 리치) | 최초 `randInt(90,200)`f / 재설정 `ENEMY_BAL.necro.cd=[270,360]`f | 평균 **5.3초마다 1회** = 11.4회/분 | 최대 2구 |
| 슬라임 분열 | 사망 시 100% | — | slimeling ×2 |

단, `updateEnemyBehavior()`에 거리 스로틀이 있다.
```js
if (d > 760 && (G.frame + e.id) % 4 !== 0) return false;
```
플레이어에게서 760 밖이면 부활 쿨다운이 4프레임에 1번만 돌아 **실효 주기가 4배(≈21초)로 늘어난다.** 소환(`e.summonT`)은 `updateEnemies`의 이동 분기에 있어 스로틀을 받지 않는다.

**도감이 플레이어에게 약속하는 것** (`GUIDE_STR`)
- 네크로맨서: "거리를 유지하며 주변에 **언데드를 소환**. 빠르게 제거 권장."
- 슬라임: "처치하면 **작은 슬라임 2마리로 분열**. 광역기로 한 번에 처리."
- 리치: "**냉기 마법**을 원거리로 발사하는 해골 대마법사." — **부활은 도감에 적혀 있지 않다.**

즉 **소환은 네크로맨서의 공개된 정체성**(도감이 "빠르게 제거 권장"이라고 우선 처치를 지시한다), **분열은 슬라임의 공개된 정체성**이다. 리치의 부활은 숨은 기능이다.

`ENEMY_ROLE`에서도 `necromancer:'support'`로 분류되어 있고, REPORT-02A에서 `support` 역할군의 정의는 "**다른 적을 강화한다 — 방치하면 다른 모든 적의 위험도가 올라간다**"였다. 소환이 막히면 이 역할군 정의가 성립하지 않는다.

---

## 2. 시뮬레이션 — 인구 분포 (400 seed, 현재 게이트 240)

> kill/s 가정 기반 추정치다. 실제 플레이 측정값이 아니다.
> 재현성: 동일 seed 재실행 결과 완전 일치 확인.
> 변이 배정: T1 없음 · T2 블러드문 · T3 독성강우 · T4 결빙지대 · T5 무덤기상 (`mutationForTier`)

| kill/s | Tier | 평균 pop | 최소 | 최대 | **≥240 시간** | ≥270 시간 | ≥290 시간 | =300 시간 | 300 도달 |
|---|---|---|---|---|---|---|---|---|---|
| 2 | 1 | 235.9 | 115.9 | 300 | 75.2% | 72.4% | 69.6% | 68.0% | 400/400 |
| 2 | 2 | 244.7 | 200.0 | 300 | 78.3% | 76.2% | 74.3% | 73.2% | 400/400 |
| 2 | 3 | 249.7 | 240.9 | 300 | 80.1% | 78.4% | 76.9% | 76.0% | 400/400 |
| 2 | 4 | 255.1 | 270.8 | 300 | **81.3%** | 80.1% | 78.9% | 78.2% | 400/400 |
| 2 | 5 | 261.1 | 294.3 | 300 | **82.8%** | 81.3% | 80.3% | 79.7% | 400/400 |
| 5 | 1 | 205.9 | 53.9 | 280.3 | 70.1% | 40.7% | 0.0% | 0.0% | 0/400 |
| 5 | 2 | 228.4 | 118.3 | 297.1 | 74.8% | 70.4% | 53.6% | 0.0% | 11/400 |
| 5 | 3 | 238.8 | 173.2 | 300 | 77.3% | 73.9% | 69.4% | 65.4% | 400/400 |
| 5 | 4 | 244.5 | 230.7 | 300 | **79.6%** | 77.2% | 74.6% | 72.7% | 400/400 |
| 5 | 5 | 251.2 | 265.5 | 300 | **81.1%** | 79.8% | 78.0% | 76.7% | 400/400 |
| 10 | 1 | 163.5 | 14.2 | 246.2 | 7.8% | 0.0% | 0.0% | 0.0% | 0/400 |
| 10 | 2 | 189.4 | 57.1 | 268.6 | 49.4% | 0.1% | 0.0% | 0.0% | 0/400 |
| 10 | 3 | 197.0 | 91.6 | 270.5 | 67.1% | 0.5% | 0.0% | 0.0% | 0/400 |
| 10 | 4 | 209.2 | 154.7 | 274.0 | **75.5%** | 4.3% | 0.0% | 0.0% | 0/400 |
| 10 | 5 | 226.1 | 226.8 | 286.1 | **79.2%** | 73.0% | 0.0% | 0.0% | 0/400 |

**핵심**: Tier 4/5는 **10 kill/s에서도 전체 시간의 75~79%를 240 위에서 보낸다.** 평균 인구가 209~226으로 상한과 한참 떨어져 있어도, 초반 60초를 빼면 사실상 상시 240 초과다.

---

## 3. 시뮬레이션 — 파생 스폰 발동 / 차단 (현재 게이트 240)

숫자는 300초 1판당 평균 횟수다.

### 2 kill/s
| Tier | 네크로 소환 시도/차단/생성 | 차단율 | 부활 시도/시체없음/차단/통과 | 차단율 | 분열 시도/차단 | 차단율 |
|---|---|---|---|---|---|---|
| 3 | 0 / 0 / 0 | — | 0 / 0 / 0 / 0 | — | 17.7 / 17.7 | **100.0%** |
| 4 | 85.8 / 85.8 / **0** | **100.0%** | 95.8 / 0 / 95.8 / **0** | **100.0%** | 16.1 / 16.1 | **100.0%** |
| 5 | 79.0 / 79.0 / **0** | **100.0%** | 137.0 / 0 / 137.0 / **0** | **100.0%** | 15.6 / 15.6 | **100.0%** |

### 5 kill/s
| Tier | 네크로 소환 시도/차단/생성 | 차단율 | 부활 시도/차단/통과 | 차단율 | 분열 시도/차단 | 차단율 |
|---|---|---|---|---|---|---|
| 3 | 0 / 0 / 0 | — | 0 / 0 / 0 | — | 69.1 / 69.1 | **100.0%** |
| 4 | 168.1 / 168.1 / **0** | **100.0%** | 188.8 / 188.8 / **0** | **100.0%** | 62.0 / 62.0 | **100.0%** |
| 5 | 147.2 / 147.2 / **0** | **100.0%** | 265.1 / 265.1 / **0** | **100.0%** | 60.8 / 60.8 | **100.0%** |

### 10 kill/s
| Tier | 네크로 소환 시도/차단/생성 | 차단율 | 부활 시도/차단/통과 | 차단율 | 분열 시도/차단 | 차단율 |
|---|---|---|---|---|---|---|
| 3 | 0 / 0 / 0 | — | 0 / 0 / 0 | — | 159.2 / 156.0 | 98.0% |
| 4 | 193.8 / 193.8 / **0** | **100.0%** | 219.3 / 219.3 / **0** | **100.0%** | 141.3 / 141.3 | **100.0%** |
| 5 | 183.9 / 183.9 / **0** | **100.0%** | 341.4 / 341.4 / **0** | **100.0%** | 136.5 / 136.5 | **100.0%** |

**Tier 3에서는 네크로맨서·리치가 아예 등장하지 않으므로(tier≥4 해금) 시도 0이다. 슬라임 분열만 T3부터 존재하며 98~100% 차단된다.**

시뮬레이터에서 `시체없음` 차단이 0인 이유: 위치를 모델링하지 않아 사거리 260 조건을 항상 만족한다고 본 상한 추정이다. **실제로는 시체 부족이 추가 제약**이며, 브라우저 실측(§5)에서 확인한다.

---

## 4. 게이트 시나리오 비교 — 240 / 270 / 제거

> 시뮬레이터에서 게이트 값만 바꿔 비교했다. **게임 코드는 수정하지 않았다.**
> `현재 240` = 지금 그대로 · `완화 270` = 게이트만 270으로 · `제거` = 게이트 없음(최종 상한 300만 유효)

### 4-1. 2 kill/s — 게이트를 바꿔도 거의 아무 일도 일어나지 않는다

| Tier | 게이트 | 평균 pop | 최대(전seed) | =300 시간 | 300 도달 | **파생 생성** | 웨이브 스폰 | util |
|---|---|---|---|---|---|---|---|---|
| 3 | 240 | 249.7 | 300 | 76.0% | 400/400 | **0.0** | 896.5 | 92.9% |
| 3 | 270 | 249.7 | 300 | 76.0% | 400/400 | **0.0** | 896.5 | 92.9% |
| 3 | 제거 | 249.7 | 300 | 76.0% | 400/400 | **16.9** | 879.5 | 92.8% |
| 4 | 240 | 255.1 | 300 | 78.2% | 400/400 | **0.0** | 899.0 | 92.5% |
| 4 | 270 | 255.1 | 300 | 78.2% | 400/400 | **0.0** | 899.0 | 92.5% |
| 4 | 제거 | 255.1 | 300 | 78.2% | 400/400 | **18.7** | 880.3 | 92.4% |
| 5 | 240 | 261.1 | 300 | 79.7% | 400/400 | 10.5 | 888.5 | 92.6% |
| 5 | 270 | 261.1 | 300 | 79.7% | 400/400 | 10.5 | 888.5 | 92.6% |
| 5 | 제거 | 261.1 | 300 | 79.7% | 400/400 | **29.1** | 869.9 | 92.5% |

**평균 인구가 소수점 첫째 자리까지 완전히 동일하다.** 이미 상한 300이 전부를 막고 있어 게이트를 풀어도 들어갈 자리가 없다. 게이트를 없애도 파생 생성이 0 → 17~29에 그치는데, 이건 상한에 도달하기 전인 초반 60초에서만 일어난 것이다.
(T5의 `240` 열에 10.5가 있는 이유: Tier 5의 변이가 무덤기상이고 그 게이트는 240이 아니라 280이기 때문이다.)

### 4-2. 5 kill/s — 마찬가지로 게이트가 병목이 아니다

| Tier | 게이트 | 평균 pop | 최대(전seed) | =300 시간 | 300 도달 | **파생 생성** | 웨이브 스폰 |
|---|---|---|---|---|---|---|---|
| 3 | 240 | 238.8 | 300 | 65.4% | 400/400 | **0.0** | 1728.5 |
| 3 | 270 | 238.8 | 300 | 65.4% | 400/400 | **0.0** | 1728.5 |
| 3 | 제거 | 238.8 | 300 | 65.4% | 400/400 | **64.6** | 1663.9 |
| 4 | 240 | 244.5 | 300 | 72.7% | 400/400 | **0.0** | 1752.8 |
| 4 | 270 | 244.5 | 300 | 72.7% | 400/400 | **0.0** | 1752.8 |
| 4 | 제거 | 244.5 | 300 | 72.7% | 400/400 | **76.2** | 1676.7 |
| 5 | 240 | 251.2 | 300 | 76.7% | 400/400 | 26.3 | 1746.9 |
| 5 | 270 | 251.2 | 300 | 76.7% | 400/400 | 26.3 | 1746.9 |
| 5 | 제거 | 251.2 | 300 | 76.7% | 400/400 | **100.6** | 1672.6 |

**270으로 완화해도 파생 생성이 0에서 전혀 늘지 않는다.** 인구가 이미 270도 넘어 있기 때문이다(≥270 시간 T4 77.2% · T5 79.8%).

### 4-3. 10 kill/s — 여기서만 게이트가 진짜 병목이고, 여기서만 위험도 생긴다

| Tier | 게이트 | 평균 pop | 최대 pop | 최대(전seed) | ≥290 시간 | =300 시간 | **300 도달** | **파생 생성** | 웨이브 스폰 | util |
|---|---|---|---|---|---|---|---|---|---|---|
| 3 | 240 | 197.0 | 270.5 | 278 | 0.0% | 0.0% | **0/400** | 6.4 | 2969.5 | 90.1% |
| 3 | **270** | **202.3** | 274.4 | 278 | 0.0% | 0.0% | **0/400** | **251.3** | 2728.0 | 90.2% |
| 3 | 제거 | 202.8 | 279.1 | 289 | 0.0% | 0.0% | **0/400** | 272.9 | 2709.5 | 90.2% |
| 4 | 240 | 209.2 | 274.0 | 281 | 0.0% | 0.0% | **0/400** | **0.0** | 3027.7 | 90.4% |
| 4 | **270** | **213.5** | 279.8 | 288 | 0.0% | 0.0% | **0/400** | **191.8** | 2839.1 | 90.7% |
| 4 | 제거 | **221.6** | **300.0** | **300** | **24.6%** | **11.8%** | **400/400** | 467.9 | 2589.2 | 91.4% |
| 5 | 240 | 226.1 | 286.1 | 293 | 0.0% | 0.0% | **0/400** | 197.6 | 2908.6 | 91.2% |
| 5 | 270 | 226.1 | 286.1 | 293 | 0.0% | 0.0% | **0/400** | 198.0 | 2908.2 | 91.2% |
| 5 | 제거 | **234.4** | **300.0** | **300** | **39.1%** | **16.4%** | **400/400** | 496.6 | 2628.8 | 92.1% |

**이 표가 이번 검증의 핵심이다.**

- **게이트 제거는 위험하다.** Tier 4/5에서 `0/400 → 400/400`으로 상한 도달이 뒤집힌다. 지금은 10 kill/s면 상한에 절대 닿지 않는데, 제거하면 항상 닿는다. 최대 인구도 274 → 300, 286 → 300.
- **270 완화는 안전하다.** Tier 3은 파생 6.4 → 251.3, Tier 4는 0 → 191.8로 살아나면서 **상한 도달은 여전히 0/400**이고 최대 인구도 274 → 280 수준이다.
- **Tier 5는 270으로도 거의 안 살아난다** (197.6 → 198.0). Tier 5의 ≥270 시간이 73.0%라 270 게이트도 대부분 막기 때문이다.
- **파생 스폰은 웨이브 스폰을 밀어낸다.** 게이트 제거 시 Tier 4에서 웨이브 스폰 3027.7 → 2589.2 (**−14.5%**). 위협 예산으로 설계한 구성이 파생 스폰에 자리를 빼앗긴다. 예산을 직접 쓰진 않지만 **개체 슬롯을 통해 경쟁한다**.

### 4-4. 게이트 완화가 인구를 얼마나 밀어 올리는가

| Tier | kill/s | 240 평균 | 270 평균 | 제거 평균 | **240→제거 증가** | 240 파생 | 제거 파생 |
|---|---|---|---|---|---|---|---|
| 3 | 2 | 249.7 | 249.7 | 249.7 | **+0.0** | 0.0 | 16.9 |
| 3 | 5 | 238.8 | 238.8 | 238.8 | **+0.0** | 0.0 | 64.6 |
| 3 | 10 | 197.0 | 202.3 | 202.8 | **+5.7** | 6.4 | 272.9 |
| 4 | 2 | 255.1 | 255.1 | 255.1 | **+0.0** | 0.0 | 18.7 |
| 4 | 5 | 244.5 | 244.5 | 244.5 | **+0.0** | 0.0 | 76.2 |
| 4 | 10 | 209.2 | 213.5 | 221.6 | **+12.3** | 0.0 | 467.9 |

**파생 스폰이 인구를 폭발시키는 시나리오는 확인되지 않았다.** 최대 증가폭이 +12.3명(T4 10 kill/s, 게이트 완전 제거)이다. 다만 그 +12.3이 평균 209 → 222로 올리면서 **최대치를 300에 닿게 만든다**는 점이 중요하다. 평균보다 꼬리가 문제다.

---

## 5. 브라우저 실측

> **방법**: 실제 `index.html`을 Playwright로 띄우고, 페이지 안에서 `spawnEnemy` / `necroSummon` / `raiseDead` / `killEnemy` / `applyMutationOnKill` / `addZone` / `updateZones` 를 **계측 래퍼로 감싸 호출 횟수만 세었다.** 원본 함수는 그대로 호출하며, 측정이 끝나면 래퍼를 제거한다. **`index.html` 파일은 수정하지 않았다** (§7 무결성 확인).
> 각 티어 300초 완주(18,000프레임). 플레이어는 `hp = maxhp` 고정으로 죽지 않게 해서 300초를 끝까지 돌렸다 — 인구/파생 스폰 측정이 목적이므로 사망으로 런이 끊기면 안 된다.
> 두 가지 빌드로 측정했다. **약한 빌드** = 시작 무기 1개만(패시브 없음), **강한 빌드** = 무기 5종 Lv7 + 패시브(dmg5/speed4/proj3/crit3).

### 5-1. 실측 킬레이트와 인구

| 빌드 | Tier | 실측 kill/s | 평균 pop | 최소 | 최대 | **≥240 시간** | ≥270 | ≥290 | **=300 시간** |
|---|---|---|---|---|---|---|---|---|---|
| 약함 | 1 | 6.98 | 195.1 | 160 | 236 | 27.5% | 1.0% | 0% | 0% |
| 약함 | 2 | 8.01 | 210.5 | 93 | **300** | 47.9% | 6.9% | 4.7% | **3.1%** |
| 약함 | 3 | 9.83 | 210.9 | 173 | 277 | 44.8% | 0.9% | 0% | 0% |
| 약함 | 4 | 9.76 | 222.4 | 214 | 286 | **61.4%** | 8.6% | 0% | 0% |
| 약함 | 5 | **5.36** | **252.0** | 102 | **300** | **82.7%** | 54.5% | 38.8% | **37.8%** |
| 강함 | 1 | 10.68 | 168.1 | 101 | 237 | 0% | 0% | 0% | 0% |
| 강함 | 2 | 12.23 | 184.2 | 169 | 247 | 4.0% | 0% | 0% | 0% |
| 강함 | 3 | 12.85 | 193.6 | 201 | 253 | 14.3% | 0% | 0% | 0% |
| 강함 | 4 | 15.24 | 199.9 | 204 | 252 | 23.7% | 0% | 0% | 0% |
| 강함 | 5 | 18.46 | 207.9 | 206 | 258 | 42.8% | 0% | 0% | 0% |

**약한 빌드 Tier 5가 최악의 구간이다.** 킬레이트가 5.36/s로 Tier 4(9.76)보다 오히려 **떨어진다** — Tier 5 적 HP를 시작 무기로 못 따라간다. 그 결과 평균 인구 252, **전체 시간의 37.8%를 상한 300에 붙어서** 보낸다. `spawnEnemy` 거부 12회도 여기서만 나왔다.

반대로 **강한 빌드는 어느 티어에서도 270을 넘지 않는다.** Tier 5조차 최대 258이다.

### 5-2. 파생 스폰 발동 / 차단 — 300초 1판 실측

| 빌드 | Tier | 필드 평균(네크로/리치/슬라임) | 네크로 소환 시도 / 차단 / **차단율** / 생성 | 부활 시도 / 차단 / **차단율** / 통과 | 분열 시도 / 차단 / **차단율** |
|---|---|---|---|---|---|
| 약함 | 3 | 0 / 0 / 14.4 | — (해금 전) | — (해금 전) | 172 / 125 / **72.7%** |
| 약함 | 4 | **7.5** / 0 / 10.2 | 382 / 382 / **100.0%** / **0** | 375 / 375 / **100.0%** / **0** | 151 / 127 / **84.1%** |
| 약함 | 5 | **6.6 / 4.0** / 7.2 | 342 / 342 / **100.0%** / **0** | 510 / 510 / **100.0%** / **0** | 95 / 94 / **98.9%** |
| 강함 | 3 | 0 / 0 / 15.1 | — (해금 전) | — (해금 전) | 215 / 50 / 23.3% |
| 강함 | 4 | 7.2 / 0 / 13.5 | 362 / 256 / **70.7%** / 212 | 294 / 209 / **71.1%** / 85 | 247 / 73 / 29.6% |
| 강함 | 5 | 6.1 / 3.0 / 12.8 | 303 / 250 / **82.5%** / 106 | 343 / 274 / **79.9%** / 69 | 275 / 108 / 39.3% |

**시뮬레이션(§3)의 100% 차단 예측이 약한 빌드에서 그대로 재현됐다.** 약한 빌드 Tier 4·5에서 네크로맨서 소환과 부활은 **300초 동안 단 한 번도 성공하지 못했다.** 시도는 각각 382·342회, 375·510회 있었다.

**여기서 가장 중요한 역전이 나온다.**

> **플레이어가 약할수록 네크로맨서의 정체성이 더 완전히 지워진다.**
> 강한 빌드는 인구를 240 아래로 눌러놔서 소환의 29%, 부활의 29%가 통과한다(T4).
> 약한 빌드는 인구가 항상 240 위라 **0%** 통과한다.
> 즉 이 게이트는 "고수만 네크로맨서를 진짜로 상대하고, 초보는 소환하지 않는 네크로맨서를 상대한다"는 결과를 만든다. 도감은 양쪽 모두에게 "빠르게 제거 권장"이라고 말한다.

### 5-3. 억제된 적 수 (상한 추정, 1판당)

| 빌드 | Tier | 소환 차단 ×2 | 부활 차단 ×2 | 분열 차단 ×2 | **합계** | 실제 `spawnEnemy` 호출 | 비율 |
|---|---|---|---|---|---|---|---|
| 약함 | 4 | 764 | ≤750 | 254 | **≤1768** | 3207 | **≤55%** |
| 약함 | 5 | 684 | ≤1020 | 188 | **≤1892** | 1976 | **≤96%** |
| 강함 | 4 | 512 | ≤418 | 146 | ≤1076 | 4819 | ≤22% |
| 강함 | 5 | 500 | ≤548 | 216 | ≤1264 | 5779 | ≤22% |

부활은 사거리 260 안에 시체가 있어야 하므로 상한값이다. **약한 빌드 Tier 5에서는 게이트가 막은 양이 실제로 스폰된 양과 맞먹는다.** 게이트를 그냥 풀면 인구가 어디로 갈지는 §4-3이 이미 답했다 — 상한 300에 상시 붙는다. **즉 게이트는 지금 실제로 안전장치 역할을 하고 있다.** 없애면 안 된다.

### 5-4. 같은 종류의 파생 스폰인데 상한이 다르다 — 코드 내부 불일치

변이 파생 스폰(`applyMutationOnKill`)은 게이트가 **240이 아니라 280**이다(`MUTATION_BALANCE.bloodmoon.cap = 280`, `graverising.cap = 280`).

| 빌드 | Tier | 변이 | 시도 | 차단 | **차단율** | 생성 |
|---|---|---|---|---|---|---|
| 약함 | 2 | 블러드문 | 2128 | 56 | **2.6%** | 275 |
| 약함 | 5 | 무덤기상 | 1479 | 136 | **9.2%** | 136 |
| 강함 | 2 | 블러드문 | 3215 | 0 | **0.0%** | 457 |
| 강함 | 5 | 무덤기상 | 4751 | 0 | **0.0%** | 454 |

**상한 40 차이가 "거의 항상 작동"과 "전혀 작동 안 함"을 가른다.** 변이 파생은 약한 빌드 Tier 5에서도 90.8%가 통과하는데, 같은 판의 네크로맨서 소환은 0%다.

그리고 **보스의 소환에는 인구 게이트가 아예 없다.** `BOSS_AI.boss`(NECRO TYRANT)와 미니보스의 소환은 `addZone({kind:'raise', spawn:...})` → `resolveZone()` → `spawnEnemy()` 경로인데, `resolveZone()`에는 `SPAWN_CAP.derived` 검사가 없다(`index.html:5065`). 최종 상한 300만 본다.

정리하면 게임 안에 **"언데드를 소환한다"는 똑같은 연출이 세 가지 다른 규칙으로 돌아가고 있다.**

| 소환 주체 | 게이트 | 약한 빌드 T5 실측 작동률 |
|---|---|---|
| 필드 네크로맨서 / 리치 | `popN > 240` | **0%** |
| 변이(블러드문·무덤기상) | `popN < 280` | **90.8%** |
| 보스 / 미니보스 | **없음** (300만) | 100% |

### 5-5. 설계 의도 대비 실제 발동 주기 검증

| 항목 | 코드 상수에서 계산한 설계값 (§1) | 실측 | 일치 |
|---|---|---|---|
| 네크로맨서 소환 시도 주기 | 평균 5.7초 | 약함 T4: 382시도 ÷ 필드평균 7.5마리 ÷ 300초 = **5.9초** | ✓ |
| 부활 시도 주기 | 평균 5.3초 (거리 밖이면 ×4) | 약함 T5: 510시도 ÷ (6.6+4.0)마리 ÷ 300초 = **6.2초** | ✓ |

**시도 자체는 설계대로 정확히 일어나고 있다.** 막히는 것은 마지막 관문뿐이다. 즉 이건 "쿨다운이 길어서 안 보인다"가 아니라 **"설계대로 발동했는데 인구 게이트가 결과만 버린다"**이다.

한 가지 더 — `necroSummon()`은 게이트보다 **연출이 먼저다**.
```js
function necroSummon(e) {
  ring(e.x, e.y, 44, '#9a4fff', 28); if (Audio.S.howl) Audio.S.howl();   // ← 먼저 실행
  if (G.popN > SPAWN_CAP.derived) return;                                 // ← 그 다음 차단
  ...
}
```
약한 빌드 Tier 4 기준 **300초 동안 보라색 소환 이펙트와 하울링 소리가 382번 재생되는데 적은 0마리 나온다.** 플레이어에게는 "소환 모션은 계속 보이는데 아무 일도 안 일어나는" 상태다.

`raiseDead()`도 **쿨다운을 먼저 소모한 뒤** 게이트를 본다(`if (--e.spT > 0) return false;` → `e.spT = randInt(...)` → 게이트). 그래서 차단당해도 쿨다운은 정상적으로 리셋되어, 인구가 240 아래로 내려온 순간 바로 부활하는 게 아니라 다음 쿨다운(4.5~6초)을 또 기다린다.

### 5-6. ⚠ 해석 주의 — 차단율을 §5-1의 인구 분포로 외삽하면 안 된다

이번 측정에서 **반드시 짚어야 할 표본 편향**이 하나 나왔다. 다음 두 숫자는 같은 판(약한 빌드 T4)의 값인데 서로 맞지 않는다.

- 프레임 기준 `pop ≥ 240` 시간: **61.4%**
- 네크로 소환 실측 차단율(게이트 240): **100.0%**

게이트가 프레임에 균일하게 걸린다면 차단율도 61% 근처여야 한다. 100%가 나온 이유는 **네크로맨서가 필드에 존재하는 시간대가 인구가 높은 시간대와 겹치기 때문**이다. 61.4%에는 네크로맨서가 아직 등장하지 않은 초반 구간이 통째로 들어가 있다. 즉 **네크로맨서가 살아 있다는 조건 하에서는 인구가 사실상 항상 240 위**다.

반대 방향의 편향도 있다. 변이 파생(`applyMutationOnKill`)은 **적이 죽는 순간**에 표본되는데, 그 시점은 (a) 플레이어가 실제로 싸워서 인구를 깎고 있는 때이고 (b) `spawnEnemy` 기준으로 이미 1명이 빠진 뒤다. 약한 빌드 T5는 프레임 기준 `≥290`이 38.8%인데도 상한 280 변이의 차단율이 9.2%밖에 안 되는 것이 이 때문이다. 인구가 300에 붙어 있는 구간에서는 애초에 킬이 거의 없어서 표본이 잡히지 않는다.

**결론: 게이트 값을 270이나 280으로 바꿨을 때의 차단율은 §5-1의 `≥270`·`≥290` 칸에서 계산해 낼 수 없다. 반드시 그 값으로 다시 측정해야 한다.** §6-6의 2단계가 이것이다. 이번 검증에서 직접 측정한 게이트 값은 **240 / 270 / 제거** 세 가지뿐이고, **280은 측정하지 않았다.**

---

## 6. 최종 보고

### 1. 결론

**문제는 실재한다. 다만 원래 의심했던 방향과 다르다.**

- ❌ "게이트를 풀면 인구가 폭발한다" → **사실이 아니다.** 평균 인구 증가폭은 최대 +12.3명(§4-4)이다.
- ❌ "게이트가 성능을 지키고 있다" → **사실이 아니다.** 완화해도 프레임당 비용 증가는 0.011~0.025%다(§6-4).
- ✅ **"설계된 적의 정체성이 조용히 사라진다"** → **사실이다.** 약한 빌드 Tier 4·5에서 네크로맨서 소환과 리치 부활의 **차단율이 정확히 100.0%**다. 300초 동안 단 한 마리도 소환되지 않는다.
- ✅ **"게이트는 안전장치로도 실제 작동 중이다"** → **이것도 사실이다.** 완전히 제거하면 Tier 4·5가 상한 도달 `0/400 → 400/400`으로 뒤집힌다(§4-3). **그러므로 제거는 답이 아니다.**

그리고 가장 중요한 것:

> **이 게이트는 실력이 낮은 플레이어에게만 발동한다.**
> 강한 빌드는 인구를 240 아래로 눌러서 소환의 29%가 통과한다. 약한 빌드는 0%다.
> **초보일수록 네크로맨서가 소환을 안 한다.** 도감은 양쪽에 똑같이 "빠르게 제거 권장"이라고 적혀 있다.

부수적으로 확인된 코드 내부 불일치(§5-4): 같은 "언데드 소환" 연출이 **필드 적은 240 게이트, 변이는 280 게이트, 보스는 게이트 없음**으로 세 갈래다. 그리고 `SPAWN_CAP.derived = 240`이라는 숫자는 **코드 주석에도 커밋 히스토리에도 근거가 없다.** `2add48f`(2026-07-23)에서 리터럴로 처음 들어왔다.

---

### 2. 핵심 수치

#### 2-1. Tier × 킬레이트별 파생 스폰 차단율 (브라우저 실측, 300초 1판)

| Tier | 빌드 | 실측 kill/s | 평균 pop | ≥240 시간 | **네크로 소환 차단율** | **부활 차단율** | **분열 차단율** |
|---|---|---|---|---|---|---|---|
| 1 | 약함 | 6.98 | 195.1 | 27.5% | — (미등장) | — | — |
| 1 | 강함 | 10.68 | 168.1 | 0% | — | — | — |
| 2 | 약함 | 8.01 | 210.5 | 47.9% | — (미등장) | — | — |
| 2 | 강함 | 12.23 | 184.2 | 4.0% | — | — | — |
| 3 | 약함 | 9.83 | 210.9 | 44.8% | — (미해금) | — | **72.7%** |
| 3 | 강함 | 12.85 | 193.6 | 14.3% | — | — | 23.3% |
| **4** | **약함** | 9.76 | 222.4 | **61.4%** | **100.0%** (382/382) | **100.0%** (375/375) | **84.1%** |
| 4 | 강함 | 15.24 | 199.9 | 23.7% | 70.7% (256/362) | 71.1% (209/294) | 29.6% |
| **5** | **약함** | **5.36** | **252.0** | **82.7%** | **100.0%** (342/342) | **100.0%** (510/510) | **98.9%** |
| 5 | 강함 | 18.46 | 207.9 | 42.8% | 82.5% (250/303) | 79.9% (274/343) | 39.3% |

#### 2-2. 시뮬레이션 교차검증 (400 seed, kill/s 가정)

| Tier | 2 kill/s | 5 kill/s | 10 kill/s |
|---|---|---|---|
| 3 (분열만) | 100.0% | 100.0% | 98.0% |
| 4 (소환/부활) | **100.0% / 100.0%** | **100.0% / 100.0%** | **100.0% / 100.0%** |
| 5 (소환/부활) | **100.0% / 100.0%** | **100.0% / 100.0%** | **100.0% / 100.0%** |

시뮬레이션은 Tier 4·5를 전 구간 100% 차단으로 예측했고, **약한 빌드 실측이 이를 그대로 재현했다.** 강한 빌드(15~18 kill/s)는 시뮬레이션이 다루지 않은 구간이며 여기서만 70~82%로 떨어진다.

#### 2-3. 설계 의도 대비 실제

| 항목 | 설계값 | 시도 실측 | 성공 실측 (약함 T4/T5) |
|---|---|---|---|
| 네크로맨서 소환 | 5.7초마다 walker ×2 | **5.9초** ✓ 정상 | **0회 / 0회** |
| 리치·네크로 부활 | 5.3초마다 최대 2구 | **6.2초** ✓ 정상 | **0회 / 0회** |
| 슬라임 분열 | 사망 시 100% | 100% ✓ 정상 | 15.9% / 1.1% |

**시도는 설계대로 100% 정확히 일어난다. 마지막 관문에서만 버려진다.**

#### 2-4. 같은 파생 스폰, 다른 상한

| 주체 | 게이트 | 약한 빌드 T5 통과율 |
|---|---|---|
| 필드 네크로맨서 / 리치 | `popN > 240` | **0.0%** |
| 변이 (블러드문 · 무덤기상) | `popN < 280` | **90.8%** (1479 중 1343) |
| 보스 / 미니보스 소환 | **없음** | 100% |

#### 2-5. 게이트 값 변경 시뮬레이션 (400 seed, 10 kill/s)

| Tier | 게이트 | 평균 pop | 최대 pop | **상한 300 도달** | **파생 생성** | 웨이브 스폰 |
|---|---|---|---|---|---|---|
| 3 | 240 (현재) | 197.0 | 278 | **0/400** | 6.4 | 2969.5 |
| 3 | **270** | 202.3 | 278 | **0/400** | **251.3** | 2728.0 |
| 3 | 제거 | 202.8 | 289 | **0/400** | 272.9 | 2709.5 |
| 4 | 240 (현재) | 209.2 | 281 | **0/400** | **0.0** | 3027.7 |
| 4 | **270** | 213.5 | 288 | **0/400** | **191.8** | 2839.1 |
| 4 | 제거 | **221.6** | **300** | **400/400** ⚠ | 467.9 | 2589.2 |
| 5 | 240 (현재) | 226.1 | 293 | **0/400** | 197.6* | 2908.6 |
| 5 | 270 | 226.1 | 293 | **0/400** | 198.0* | 2908.2 |
| 5 | 제거 | **234.4** | **300** | **400/400** ⚠ | 496.6 | 2628.8 |

\* Tier 5의 197.6은 대부분 변이(무덤기상, 상한 280)에서 나온 것이고 네크로/리치 몫은 0이다.

**2 kill/s와 5 kill/s에서는 게이트를 240→270으로 바꾸든 아예 없애든 평균 인구가 소수점 첫째 자리까지 완전히 동일하다(§4-1, §4-2).** 이미 상한 300이 전부를 막고 있기 때문이다.

---

### 3. 원인

**세 가지가 겹쳐 있다.**

**(1) 240이라는 숫자에 근거가 없다.**
`SPAWN_CAP.derived = 240`은 최종 상한 300의 80%다. 그런데 Tier 4·5의 **정상 운영 인구 자체가 이미 200~252**다(§5-1). 즉 게이트가 "붐빌 때만 멈춘다"가 아니라 **"Tier 4·5의 평상시"에 걸려 있다.** 커밋 `2add48f`에서 주석 없는 리터럴로 들어왔고, PHASE H-2에서 슬라임 분열까지 260 → 240으로 통합되며 더 조여졌다(의도된 변경이 아니라 통합의 부작용).

**(2) 게이트 위치가 잘못됐다 — 연출 뒤, 쿨다운 소모 뒤.**
```js
function necroSummon(e) {
  ring(...); Audio.S.howl();          // 연출이 먼저 나간다
  if (G.popN > SPAWN_CAP.derived) return;   // 그 다음에 막는다
```
```js
function raiseDead(e) {
  if (--e.spT > 0) return false;
  e.spT = randInt(B.cd[0], B.cd[1]);        // 쿨다운을 먼저 태운다
  if (!G.corpses.length || G.popN > SPAWN_CAP.derived) return false;   // 그 다음에 막는다
```
그래서 (a) 약한 빌드 T4에서 **소환 이펙트·하울링이 382번 재생되는데 적은 0마리**, (b) 차단당해도 쿨다운은 소모되어 **인구가 내려가도 즉시 부활하지 않고 4.5~6초를 또 기다린다.**

**(3) 인구 상한 하나로 두 가지 다른 목적을 동시에 처리하려 한다.**
`SPAWN_CAP.derived`는 지금 **성능 보호**와 **밸런스 조절**을 겸하고 있다. 그런데 측정해 보면 성능 쪽 부담은 무시할 수준이고(§4), 실제로 하는 일은 밸런스 조절뿐이다. 그리고 그 밸런스 조절이 **약한 플레이어에게만 선택적으로 발동**하는 형태라 "적을 배우는 경험"을 실력 낮은 쪽에서 먼저 지운다.

---

### 4. 성능 위험

**결론: CPU 위험은 사실상 없다. 진짜 위험은 상한 포화(밸런스)다.**

REPORT-02B §7에서 측정한 적 1마리당 프레임 비용은 **0.00031~0.00033 ms**다.

| 시나리오 | 평균 인구 증가 | 추가 프레임 비용 | 16.7ms 예산 대비 |
|---|---|---|---|
| 240 → 270 (T4, 10 kill/s) | +4.3명 | +0.0014 ms | **0.008%** |
| 240 → 제거 (T3, 10 kill/s) | +5.7명 | +0.0018 ms | **0.011%** |
| 240 → 제거 (T4, 10 kill/s) | +12.3명 | +0.0041 ms | **0.025%** |

**최악의 경우에도 프레임 예산의 0.025%다.** 이 게이트는 성능을 지키고 있지 않다.

진짜 위험은 두 가지고, 둘 다 **게이트 완전 제거에서만** 나타난다.

- **상한 포화**: Tier 4·5가 `0/400 → 400/400`으로 뒤집힌다. 지금은 10 kill/s면 300에 절대 안 닿는데, 제거하면 항상 닿는다. 상한에 붙으면 웨이브 스폰이 거부되기 시작해 **위협 예산 설계가 무력화된다.**
- **웨이브 구성 침식**: 게이트 제거 시 Tier 4에서 웨이브 스폰 3027.7 → 2589.2 (**−14.5%**). 파생 스폰이 위협 예산을 직접 쓰진 않지만 **개체 슬롯을 통해 경쟁**한다. 설계한 티어별 적 구성이 walker/runner/slimeling으로 희석된다.

**270 완화에서는 두 위험 모두 나타나지 않는다** (상한 도달 0/400 유지, 최대 인구 274 → 288).

---

### 5. 수정안 후보

> **아래는 후보 제시일 뿐이며, 이번 단계에서 아무것도 구현하지 않았다.**

#### 후보 A — 게이트를 240 → 270으로 완화 (가장 단순)

`SPAWN_CAP.derived: 240` → `270` 한 줄.

| 항목 | 내용 |
|---|---|
| **예상 효과** | **직접 측정된 값**(시뮬레이션 10 kill/s, §2-5): T3 파생 6.4 → **251.3**, T4 **0 → 191.8**. **단 T5는 거의 안 살아난다** — 197.6 → 198.0. Tier 5의 ≥270 시간이 73%라 270 게이트도 대부분 막는다. 브라우저 실측 빌드(약함/강함)에서의 효과는 **측정하지 않았다** — §5-6의 표본 편향 때문에 §5-1의 ≥270 수치로 외삽할 수 없다. |
| **성능 위험** | 없음. +0.0014 ms/frame (예산의 0.008%). 상한 도달 0/400 유지. |
| **게임플레이 영향** | 중간 실력대에서만 네크로맨서가 되살아난다. **문제의 핵심인 "약한 플레이어에게만 100% 차단"은 해결되지 않는다.** 웨이브 스폰 −6.2%(T4). |
| **변경 범위** | 상수 1줄. 회귀 위험 최소. |

#### 후보 B — 게이트를 쿨다운·연출보다 앞으로 옮긴다 (인구 상한은 그대로)

`necroSummon()`의 게이트를 `ring`/`howl` **앞**으로, `raiseDead()`의 게이트를 `--e.spT` **앞**으로 옮긴다. `SPAWN_CAP.derived = 240`은 **건드리지 않는다.**

| 항목 | 내용 |
|---|---|
| **예상 효과** | 실제 스폰 수는 **변하지 않는다**(인구 상한 동일). 대신 (a) 헛도는 소환 이펙트·하울링 382회/판이 사라진다, (b) 쿨다운이 소모되지 않으므로 인구가 240 아래로 떨어지는 순간 **즉시** 부활·소환이 일어난다 — 지금은 4.5~6초를 더 기다린다. 강한 빌드 기준 통과율이 29% → 대략 40%대로 자연 상승할 것으로 보이나, 이건 추정이며 측정 필요. |
| **성능 위험** | **음수**(이득). 막힐 경우 이펙트·오디오 호출이 통째로 생략된다. |
| **게임플레이 영향** | 인구·난이도 곡선이 사실상 그대로라 밸런스 리스크가 가장 낮다. "소환 모션만 나오고 아무 일도 안 일어나는" 체감 버그가 사라진다. **단 약한 빌드 T4·T5의 0% 문제는 그대로 남는다.** |
| **변경 범위** | 함수 2개의 줄 순서. 단, `raiseDead()`에서 쿨다운을 소모하지 않게 바꾸면 **매 프레임 게이트를 재검사**하게 되므로 인구가 240 근처에서 진동할 때 부활이 몰릴 수 있다 — 이 점은 실측 필요. |

#### 후보 C — 파생 종류별로 상한을 분리하고, 네크로/리치를 변이와 같은 280으로 맞춘다

```
SPAWN_CAP.derived  240  →  { summon: 280, split: 260 }   (예시, 수치는 검증 대상)
```
§5-4에서 확인한 코드 내부 불일치(필드 240 / 변이 280 / 보스 게이트 없음)를 정리하는 방향이다.

| 항목 | 내용 |
|---|---|
| **예상 효과** | **추정이며 미측정이다.** 변이 파생이 상한 280에서 약한 빌드 T5조차 90.8% 통과한다는 실측(§2-4)이 근거지만, 변이는 *사망 시점*에 표본되고 네크로 소환은 *타이머*로 발동하므로 두 값을 직접 비교할 수 없다(§5-6). 방향은 "240·270보다 확실히 많이 통과한다"이며, **약한 빌드 T4·T5에서 100%가 깨지는 유일한 후보**로 보인다. 분열은 PHASE H-2 이전 값 260으로 되돌리는 셈. |
| **성능 위험** | 후보 A와 제거안의 중간. 시뮬레이션으로 280 시나리오를 **아직 측정하지 않았다** — 240/270/제거만 돌렸다. 제거안이 T4에서 400/400 상한 도달을 만들었으므로 **280이 안전한지는 반드시 추가 검증이 필요하다.** 특히 약한 빌드 T5는 이미 37.8%를 상한 300에 붙어 있어 여유가 없다. |
| **게임플레이 영향** | 가장 크다. 약한 플레이어의 Tier 4·5 난이도가 실제로 올라간다 — 지금 게이트가 사실상 **숨은 자비 장치**로 작동 중이기 때문이다(§5-3: 약한 빌드 T5에서 막은 양이 스폰된 양과 맞먹는다). 난이도 재조정이 동반될 수 있다. |
| **변경 범위** | 상수 구조 + 참조 5곳(`necroSummon`, `raiseDead`, 슬라임 분열, 그리고 변이 2곳의 통합 여부). 회귀 테스트 필요. |

---

### 6. 추천 작업 순서

**1단계 — 후보 B 먼저 (밸런스 무변경, 위험 최소)**
게이트 위치만 앞으로 옮긴다. 인구 상한과 난이도 곡선을 전혀 바꾸지 않으면서 "이펙트만 나오고 적은 안 나오는" 명백한 체감 버그를 없앤다. 이건 밸런스 판단이 필요 없는 순수한 버그 성격이다. 단 `raiseDead()`의 쿨다운 재검사 빈도 변화는 실측으로 확인해야 한다.

**2단계 — 280 시나리오 시뮬레이션 추가 (코드 무수정)**
이번 검증에서 240/270/제거 세 가지만 돌렸다. **후보 C를 판단하려면 280이 상한 300을 얼마나 자주 치는지가 반드시 필요하다.** 특히 약한 빌드 Tier 5(이미 37.8%가 상한 붙음)를 브라우저 실측으로 확인해야 한다. 여기서 400/400이 나오면 후보 C는 폐기하고 후보 A로 간다.

**3단계 — 2단계 결과에 따라 분기**
- 280이 안전하면 → **후보 C**. 필드 네크로/리치를 변이와 같은 280으로 통일해 코드 내부 불일치까지 정리한다. 다만 약한 플레이어의 T4·T5 난이도가 실제로 올라가므로, 난이도 곡선 재확인이 함께 필요하다.
- 280이 위험하면 → **후보 A**(270). 효과는 제한적이지만 상한 도달 0/400이 확인된 안전한 값이다.

**4단계 — 도감/연출 일치 확인**
어느 안을 택하든, 리치의 부활은 지금 도감에 전혀 적혀 있지 않다(§1). 부활이 실제로 작동하기 시작하면 플레이어가 "왜 죽인 적이 다시 서 있는지" 알 수 있어야 한다.

**하지 말아야 할 것 — 게이트 완전 제거.**
§4-3에서 Tier 4·5가 `0/400 → 400/400`으로 뒤집히고 웨이브 스폰이 14.5% 밀려나는 것이 확인됐다. 지금 이 게이트는 실제로 안전장치 역할을 하고 있다.

---

## 7. 무결성 확인 — 코드 무수정

이번 작업에서 게임 코드를 단 한 글자도 바꾸지 않았다.

```
$ git status --short
?? REPORT-02D-DERIVED-SPAWN-GATE.md
```

`index.html`은 변경 목록에 없다. 계측은 **페이지가 로드된 뒤 브라우저 메모리 안에서 함수를 래핑**하는 방식으로만 했고, 측정이 끝나면 원본 함수를 복구한다(`for (const k in O) window[k] = O[k];`). 시뮬레이터는 `/tmp/`에만 두었다.

금지 항목 전체 확인:

| 금지 항목 | 상태 |
|---|---|
| `index.html` 수정 | 미수정 ✓ |
| `G.popN > 240` 수정 | 미수정 ✓ |
| 240을 다른 숫자로 변경 | 미변경 ✓ |
| Hard Cap 300 변경 | 미변경 ✓ |
| Threat cost 변경 | 미변경 ✓ |
| spawn interval 변경 | 미변경 ✓ |
| WAVE_COMPOSITION 변경 | 미변경 ✓ |
| 적 HP / 공격력 변경 | 미변경 ✓ |
| 난이도 밸런스 변경 | 미변경 ✓ |
| 새로운 시스템 추가 | 없음 ✓ |
| UI / 오디오 / 로비 변경 | 없음 ✓ |
| 무기 / 캐릭터 / 보스 변경 | 없음 ✓ |
| 저장 시스템 변경 | 없음 ✓ |
