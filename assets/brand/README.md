# 국가상징 이미지

사이트에서 실제로 쓰이는 파일들입니다.

| 파일 | 쓰이는 곳 | 비고 |
|---|---|---|
| `flag.webp` | 상단 메뉴, 국호 글씨 왼쪽 (높이 24px) | 216×144 · 7KB |
| `emblem.png` | 파비콘 — 브라우저 탭이 어두울 때 | 512×512 · 흰 그림 · 투명 배경 |
| `emblem-ink.png` | 파비콘 — 브라우저 탭이 밝을 때 | 512×512 · 먹색 그림 · 투명 배경 |
| `about-hero.webp` | 소개 페이지 맨 위 | 1536×1024 · 266KB |

## 파비콘을 두 개 두는 이유

`emblem.png` 는 흰 그림에 배경이 투명합니다. 이것이 기본이며,
`media` 를 무시하는 브라우저도 이 파일을 씁니다.
다만 **밝은 탭에서는 흰 그림이 배경에 묻혀 거의 보이지 않으므로**
먹색 버전을 함께 두고 `media="(prefers-color-scheme: light)"` 로 갈라 씁니다.

## 해상도에 관하여

두 파비콘은 271×205 원본에서 만들어 512×512 로 키운 것이라
**16~20px 에서는 선이 다소 흐리게 보입니다.** 촛불·태극·연꽃에 외곽 호와
잎까지 들어간 세밀한 도안이라 작은 크기에서 뭉개지는 것이 원인입니다.
고해상도 원본(1000px 이상)을 `emblem-src.png` 로 올려 주시면
더 선명하게 다시 만들 수 있습니다.

## 원본 파일

업로드하신 원본(`flag.png` 1.4MB, `about-hero.png` 2.4MB)은 표시 크기에 비해
너무 커서 WebP로 줄여 쓰고 작업 폴더에서는 지웠습니다.
원본은 커밋 `7ee2f88` 에 그대로 남아 있어 언제든 다시 꺼낼 수 있습니다.

## 바꾸고 싶을 때

같은 이름으로 덮어쓰면 됩니다. 파비콘을 새로 만들 때는 정사각형,
배경 투명, 여백 최소로 512×512 PNG 를 권장합니다.
